# GOALS — work queue (append-only checklist)

## Standing rules — every fire obeys these, no exceptions
1. Read this file in full, then PROGRESS.md's tail (last 2–3 entries), then ROADMAP.md, then CLAUDE.md and PLAN.md (especially the Rev 2 section) before touching anything.
2. **First command of every fire:** `bash scripts/setup.sh` — `vendor/pvpoke` is gitignored and will NOT exist in a fresh clone. Nothing works without it.
3. **Verification gates:** a checkbox may be checked ONLY after its stated verify command ran clean in THIS run. Code review alone is never verification. Anything you could not actually run gets written in PROGRESS.md as "unverified: <thing>" — a false "done" is the most expensive mistake in this project.
4. `vendor/pvpoke` is read-only. Never edit it. NEVER reimplement pvpoke's battle math — load and execute pvpoke's own code (see src/engine/pvpokeLoader.js for the established pattern). A result produced by hand-rolled damage/battle formulas is wrong by definition here.
5. One bounded, complete chunk per fire (finish the ticket outright if reasonably scoped). Never leave the repo broken, even mid-task: `node --test` on the suites you touched must pass before you commit. Commit and push to `main` directly, then append a PROGRESS.md entry (what you did, current state, what's next, verified vs. not, `date -u` timestamp).
6. Blocked or ambiguous → make a documented judgment call and proceed; skip to the next ticket only if truly blocked, and say why in PROGRESS.md. Queue empty → pull from ROADMAP.md's known gaps. Stay scoped — no unrequested refactors.
7. Tickets marked (seq) depend on earlier unchecked tickets — take the first unchecked ticket unless it's explicitly parallel-safe.
8. **Run verify commands in the FOREGROUND with an explicit generous timeout (`npm test` needs ~300000–600000 ms) — NEVER as a background task you "wait" on.** A scheduled cloud run is NOT re-invoked when a background command finishes: ending your turn while a background `npm test` holds your uncommitted work means the run ends, nothing commits, and the entire fire's work is lost (this exactly happened 2026-08-21 ~19:53Z — a complete T23 implementation died uncommitted). If a long command hits the tool's default 3-minute timeout, re-run it foreground with a longer explicit timeout — do not background it.
9. **G0 — standing environment gate, EVERY fire (not a queue ticket; never gets checked off):** after `bash scripts/setup.sh`, confirm `vendor/pvpoke` is at pinned commit ea601f0a61c548f9140e4605b94a31fa97fe6aba and the suites you'll rely on pass before starting ticket work. First verified in this sandbox 2026-08-20 (42/42, no fallback needed — see PROGRESS). If the pvpoke clone ever FAILS here (network policy change): fall back to vendoring the needed subset INTO this repo — copy `src/js` + `src/data/gamemaster.json` + `src/data/groups/` + `src/data/rankings/all/overall/rankings-1500.json` + `src/data/training/` from a working clone into `vendor-static/pvpoke/`, un-ignore that path, point setup.sh and the engine's vendorRoot default at it, keep the pin recorded, and say in PROGRESS.md the fallback was taken.

## Queue
- [x] **T1 — Finish the 1v1 scoring matrix (WIP exists).** `src/scoring/index.js` is a 326-line UNVERIFIED draft from a killed session worker — review it, keep what's sound, finish per PLAN.md's Matrix interface + "Scoring & team metric" (still live for per-mon scores). Includes `loadMeta` from `vendor/pvpoke/src/data/groups/great.json` (`_shadow` suffix → base id + shadow flag; apply the group's movesets via pvpoke's own Pokemon methods on live instances) and `scoreCollection` (build every mon once; skip-with-warning on bad rows; opts.metaLimit + opts.onProgress). Write `test/scoring.test.js` (meta movesets actually applied; matrix shape per PLAN; one pair's s11 equals a direct simBattle; weighted-score arithmetic; azumarill-beats-a-weak-pick sanity on a small meta subset; full-meta run stays OUT of tests). Verify: `node --test test/scoring.test.js`.
- [x] **T2 — (seq) 3v3 engine: pvpoke Training mode headless.** Extend `src/engine/` (new file, e.g. `teamBattle.js` + loader additions) to run pvpoke's emulate/Training team battles in Node: load `src/js/pokemon/Player.js` and the needed `src/js/training/` modules into the existing vm context (same stub pattern as pvpokeLoader.js — stub only what simulation actually touches; the interface/DOM callbacks likely need inert stubs). Expose `battleTeams(ctx, {teamA, teamB, leadA, leadB, difficulty})` → `{winner, survivorsHp, summary}` with teams of 3 built via buildPokemon, shared 2-shield pools per player, highest AI difficulty; pin or document any RNG (repeat-run aggregation is acceptable if pinning is impractical — document which in src/engine/README.md). Acceptance/verify: `node --test test/teamBattle.test.js` — mirror teams across all 9 lead pairings aggregate to ~50/50 (tolerance documented); a deliberately dominant team (3 top meta mons vs 3 weak mons) wins every pairing; results deterministic under the chosen RNG policy; NO battle math reimplemented. If after honest effort emulate mode is genuinely infeasible headless: write the findings in PROGRESS.md + src/engine/README.md, check this box as "investigated — infeasible, see PROGRESS", and do T2b instead.
- [ ] **T2b — (only if T2 concluded infeasible) chained-1v1 3v3 fallback.** Per PLAN.md Rev 2 fallback paragraph: 1v1 sims chained with HP/energy carryover (pvpoke supports custom starting HP/energy), best-counter switch policy from the T1 matrix, shared-shield allocation enumeration, outcome tree. Same `battleTeams` signature so downstream tickets don't care which engine exists. Verify: `node --test test/teamBattle.test.js` (same assertions where applicable; approximation limits documented).
- [x] **T3 — Meta team pool.** `src/meta/teams.js`: load curated Great League teams from `vendor/pvpoke/src/data/training/` (explore its format first; pick the Great League set; each team: 3 speciesIds + movesets, built via buildPokemon + group/preset movesets). Fallback if training data is unsuitable: compose ~10 archetype teams from `groups/great.json` top entries, documented. Export `loadMetaTeams(ctx, opts)` → `[{id, name?, members: [3 built mons]}]`. Verify: `node --test test/metaTeams.test.js` (loads ≥8 teams, members are battle-ready, ids stable).
- [x] **T4 — (seq after T2/T2b, T3) Team evaluator + ranking.** `src/teams/index.js` per PLAN.md Rev 2 interface: candidates = C(topK from matrix scores, 3), no duplicate species per team (shadow/base = same species), each candidate battles every meta team across all 9 lead pairings via battleTeams; rank by mean win rate, tiebreak mean surviving-HP margin; per-candidate: win% vs each meta team, bestLead, hardestTeams. opts: topK (default 20 — cap total battles sensibly, document the budget math), teamCount, excludeSpecies. Orchestrator note (post-T2): pvpoke's emulate mode has a small residual player-1 side edge (see PROGRESS 17:59Z) — evaluate ALL candidates from the same fixed side (candidate = team A) so the bias cancels in relative rankings, and say in the report that absolute win% carries that constant offset. Verify: `node --test test/teams.test.js` (tiny pools: 4 candidates × 2 meta teams; known dominant team ranks first; determinism; excludeSpecies; dup-species rule).
- [x] **T5 — (seq) CLI + report.** `node src/cli.js <collection.csv> [--top N] [--topK K]` → wires importer → scoring → evaluator; terminal summary + `out/report.md` (ranked teams, win% vs each meta team, best lead per team, hardest opposing teams, per-mon 1v1 score table as appendix; collection warnings surfaced). Runs end-to-end on `fixtures/sample-pokegenie.csv`. Verify: run that exact CLI command on the fixture; assert `out/report.md` exists and names ≥1 team; plus `node --test test/cli.test.js` if written.
- [x] **T6 — (seq) E2E + README.** `test/e2e.test.js`: fixture CSV → CLI → report, sane content assertions, kept fast (metaLimit/topK small). README.md: what it is, setup (`npm run setup`), usage with a real Poke Genie export, how scoring works (1v1 matrix for pruning + real 3v3 battles for ranking), tunables, approximation notes if T2b path. Verify: `npm test` fully green + a fresh-clone walkthrough of the README commands.
- [x] **T7 — (post-MVP, from ROADMAP known-gaps) Safe-swap analysis.** Per candidate team, report which non-lead member is the safest first switch. Computed free from data `evaluateTeams` (`src/teams/index.js`) already produces — `battleTeams`'s `survivorsHp.aPerMon` gives each member's ending HP per battle; average each member's remaining-HP fraction over only the battles where it was NOT leadA (i.e. it was switched in), and pick the higher of the two non-bestLead members as `safeSwap`. No new battles, no battle math. Surfaced in `out/report.md` per team. Verify: `node --test test/teams.test.js test/cli.test.js`.
- [x] **T8 — (post-MVP, from ROADMAP known-gaps) Best Buddy detection: verify + document manual override.** The importer already recognizes a `Best Buddy`/`buddy` (Poke Genie) or `bestbuddy`/`best buddy`/`buddy` (generic) column opportunistically (`src/importer/index.js`) and `buildPokemon` already honors `bestBuddy` for the level-51 cap (`src/engine/harness.js`) — but no test exercised the header actually parsing a truthy value through to `bestBuddy: true` (only the absent-column default-false case was covered), and README didn't tell a user how to flag a Best Buddy mon at all given a stock Poke Genie export has no such column. Add positive-case tests for both CSV formats and a README section documenting the manual-override workflow. No code changes to importer/engine (already correct). Verify: `node --test test/importer.test.js`.

## Sampling initiative (2026-08-21, Jaxon's directive) — widen both sides of the matchup via weighted sampling, hold the battle budget roughly flat
Rationale: the exhaustive path (candidates = C(topK,3), opponents = the fixed curated list) couples surface area to battle count, so coverage only grows by exploding runtime. Sampling decouples them: candidate teams are sampled from a WIDE user-mon pool with meta-strong mons weighted to appear on more teams; opponent teams are sampled at random from the weighted meta pool (mixed with curated teams). Design + interfaces: PLAN.md Rev 3. All sampling seeded + deterministic; `evaluateTeams` stays UNCHANGED — the samplers are pure list generators feeding its existing `candidates`/`metaTeams` params.

- [x] **T9 — Per-species meta usage weights.** New `src/meta/usage.js`: `loadUsageWeights(ctx, opts)` → normalized positive weight per speciesId, covering every species in `groups/great.json` and the curated training teams. Deterministic base: vendored `rankings/all/overall/rankings-1500.json` score (0–100) → weight ∝ (score/100)^γ, γ tunable (document the default; pick it so top-tier mons are meaningfully likelier than fringe picks, not winner-take-all). Optional freshness layer: `scripts/refresh-usage.mjs` fetches pvpoke's LIVE GL rankings JSON (https://pvpoke.com/data/rankings/all/overall/rankings-1500.json — same shape as the vendored file; pvpoke's usage data traces to gobattlelog.com) and writes a committed snapshot `data/meta-usage.json` `{fetchedAt, source, entries}`; the loader prefers a present+parseable snapshot over vendored (accept an explicit path opt for testability), and network failure NEVER breaks a run (fallback + stderr warning; NO network in tests). Aug 2026 meta anchors for sanity (live research 2026-08-21): Clodsire, Azumarill, Galarian Stunfisk, Morpeko (Full Belly), Mandibuzz, Gastrodon, Annihilape, Feraligatr — resolve exact speciesIds via the gamemaster, drop any absent from the pinned data, and assert the rest sit above the median weight. Verify: `node --test test/metaUsage.test.js` (normalization, determinism, monotonic in score, anchors-above-median, snapshot-preference + corrupt-snapshot fallback via a temp file).
- [x] **T10 — (seq after T9) Weighted opponent-team sampler.** New `src/util/rng.js` (tiny seeded PRNG, e.g. mulberry32 — sampling machinery, not battle math; no npm deps) + `src/meta/sampleTeams.js`: `sampleOpponentTeams(ctx, {count, weights, seed, curatedRatio, curated})` → same shape as `loadMetaTeams` output (`{id, name?, members: [3 built battle-ready mons]}`). Mixture: ~curatedRatio (default ~0.4, documented) of `count` drawn without replacement from the curated training teams, remainder composed by weighted sampling of 3 DISTINCT species from the usage-weight pool (movesets via the same group/preset plumbing `loadMetaTeams` already uses; skip-and-resample species that fail to build). Stable deterministic ids under a fixed seed; label each team curated vs sampled. Verify: `node --test test/sampleTeams.test.js` (determinism under seed, distinct species per team, mixture proportion, battle-readiness same as the metaTeams test, loose distribution check: over ≥200 sampled teams, top-quartile-weight species appear ≳2× bottom-quartile).
- [x] **T10b — (seq after T10) Community curated teams → opponent pool.** `data/meta-teams-community.json` is committed: 33 Great League teams — 17 recommended by top GBL players for Season 27 (YonkouJean infographic via Reddit) plus 16 tagged `tier: "off-meta"` (JP community cup parties + player parties; provenance + extraction notes inside the file; `uncertain`/`notes` fields are advisory for humans — load those entries like any other). `tier: "off-meta"` teams join the curated pool at a reduced, documented sampling weight (e.g. half) relative to untagged (meta) teams; the exhaustive path may cap how many off-meta teams it includes (document the choice). Extend the curated-pool source so BOTH the exhaustive path and `sampleOpponentTeams`' curated mix draw from vendor training presets PLUS this file: build each member via the existing buildMetaMon/recommended-moveset plumbing (these ids are NOT all in groups/great.json — the builder must fall back to pvpoke's recommended moveset for any legal speciesId, which buildPokemon already does); skip-with-stderr-warning any speciesId that fails to resolve in the gamemaster and drop a team whole if ANY member fails — 2 mons isn't a team (a data edit must never crash a run); namespace ids (e.g. `community:yonkoujean`) and carry name + tier through so reports can label them. Verify: `node --test test/metaTeams.test.js` (extended: community file loads, ≥26 of the 33 teams resolve fully battle-ready (a few JP ids like arctibax may legitimately be absent from the pinned gamemaster), a bogus speciesId in a temp-file copy drops that team with a warning instead of throwing, ids stable across reloads, off-meta tier carried through).
- [x] **T11 — (seq after T9; parallel-safe with T10) Weighted candidate-team sampler.** New `src/teams/sample.js`: `sampleCandidateTeams({matrix, pool, weights, count, seed, excludeSpecies})` → unique `[[3 userMonKeys]…]`, no duplicate species within a team (shadow/base = same species, same rule as the evaluator). `pool` = deduped best-per-species user mons — reuse the CLI's `dedupeBestPerSpecies` (moving it to a small shared module is allowed; keep CLI behavior identical). P(mon) ∝ a blend of its normalized 1v1 matrix score and its species' T9 usage weight (so Jaxon's OWN meta mons land on more candidate teams); document the blend formula and its tunable strength. Graceful cap when C(pool,3) < count. Verify: `node --test test/sampleCandidates.test.js` (determinism, team uniqueness, no-dup-species, excludeSpecies honored, count/cap behavior, monotonicity: over a large sample a clearly higher-weight mon appears on more teams than a clearly lower one).
- [x] **T12 — (seq after T10+T11) Wire sampling into CLI + report + E2E.** `src/cli.js`: sampling becomes the DEFAULT path (`--exhaustive` restores the old C(topK,3)+curated-only behavior; existing `--topK`/`--meta` keep their meaning there). New flags: `--candidates N`, `--opponents M`, `--pool P`, `--seed S`, `--curated-ratio R` — pick defaults so the full fixture CLI run stays ≤ ~3 min in the sandbox (battles = N×M×9 at ~172ms each; document the budget math in the cli.js header AND README). Report: Settings line gains sampling params + seed; opponent list labels curated vs sampled; per-opponent win% tables unchanged. Update `test/cli.test.js` + `test/e2e.test.js` (tiny knobs, still fast; cover the sampled AND `--exhaustive` paths at least once each) + README (new flags, how the weighting works, reproducibility via `--seed`, refresh-usage script). Verify: `npm test` fully green + acceptance run `node src/cli.js fixtures/sample-pokegenie.csv` (sampled default) → exit 0, report exists and names ≥1 team, measured wall time recorded in PROGRESS.md and within budget.

## Sim throughput (2026-08-21, Jaxon's directive) — make battles faster so simulations can go bigger
Jaxon wants larger runs (tournament-scale: 10^5–10^6 battles). Speed must NEVER come from touching pvpoke's battle math (vendor stays read-only, standing rule 4) — only from how we drive it: per-battle overhead in OUR harness code, and parallelism across CPU cores. Hard invariant for both tickets: same seed → same rankings; single-thread results must stay identical to today's, and parallel results identical to serial (battles already have independent per-battle seeds, so scheduling must not change outcomes — only order of execution).

- [x] **T14 — Profile + benchmark harness + single-thread wins.** New `scripts/bench.mjs`: times N repeatable battles (fixed teams + seeds) and prints ms/battle + breakdown (build vs battle vs our wrapper overhead); commit the baseline numbers to PROGRESS.md (sandbox) — Jaxon's local baseline is ~73ms/battle, sandbox ~172ms. Profile (node --cpu-prof or --prof, or manual phase timers) where time actually goes in `battleTeams`/`src/engine/teamBattle.js` OUR-code paths (vm boundary crossings, per-battle object construction, console silencing, seed derivation, anything re-done per battle that could be done once per matchup/process). Apply only wins that keep `npm test` green and per-battle results bit-identical (assert: a fixed battle set produces identical winners/survivorsHp before vs after — write this as a test or bench assertion). Document each win + measured effect in PROGRESS.md; a null result ("profiled, our overhead is X%, engine dominates, nothing safe to squeeze") is a VALID outcome — record it and check the box. Verify: `node --test test/bench.test.js` (bench harness runs a tiny N deterministically) + `npm test` green + bench numbers in PROGRESS.
- [x] **T15 — (seq after T14) Parallel battle executor via worker_threads.** New `src/engine/parallel.js`: a worker-pool executor (`runBattles(specs, {threads})` → results in spec order) where each worker thread boots its own vm engine context once and processes battle specs {teamA, teamB, leadA, leadB, seed} via the existing battleTeams — no engine/vendor changes, no battle-math changes. Threads default `max(1, os.cpus().length - 1)`, overridable (`--threads`, env). Determinism: results keyed to spec order, identical to a serial run of the same specs (assert in tests). Integration: `evaluateTeams`' PUBLIC interface stays frozen (PLAN Rev 3) but its internals MAY delegate battle execution to the executor behind an opt-in `opts.threads`; `scripts/tournament.mjs` gains `--threads` too. Mind sandbox reality: cloud vCPUs are few — correctness verified in-sandbox, speedup primarily materializes on Jaxon's multi-core Mac (document measured speedup at threads=2 in-sandbox + note expected local scaling). Team-building happens in the main thread or per-worker (pick one, document why; mons don't serialize across threads — send specs as plain data e.g. speciesId/ivs/moves and rebuild per worker, cached). Verify: `node --test test/parallel.test.js` (serial-vs-parallel identical results incl. survivorsHp; thread-count edge cases 1 and 4; a worker crash surfaces as a clean error not a hang) + `npm test` green + bench-with-threads numbers in PROGRESS.
- [x] **T15b — (seq after T15) Wire `opts.threads`/`--threads` into `evaluateTeams` and `scripts/tournament.mjs`.** T15 shipped `runBattles` as a fully-tested standalone executor but did NOT wire it into either caller — both drive `battleTeams` directly in a loop, and both only have `{speciesId, name, pokemon}` on hand per member (from `matrix.builtMons` / meta-team members), not the `{ivs, shadow, bestBuddy}` a `parallel.js` `MonSpec` needs. `bestBuddy` in particular is never stored on a built `Pokemon` instance (only consumed at build time for the level-51 cap), so it can't be recovered after the fact — the raw params must be carried alongside `pokemon` through `src/scoring/index.js`'s `builtMons` shape and through meta/sampled team members, then threaded down to wherever `evaluateTeams`/`runFunnelStage` build `teamA`/`teamB`. Keep `evaluateTeams`' public interface frozen (PLAN Rev 3) — add `opts.threads` as a new optional field, default undefined = today's serial behavior, unchanged results either way. Verify: `node --test test/teams.test.js` (serial vs `opts.threads` produce identical win rates, ranking order, and bestLead; exact avgHpMargin exempted — pvpoke's `resetMoves()` order-sensitivity is a pre-existing engine characteristic, documented in src/engine/README.md "Known limitation") + `npm test` green. **[Orchestrator 2026-08-21T09:35Z: acceptance bar amended to what pvpoke's engine actually guarantees — the meaningful bar (identical rankings/win rates) HOLDS and was verified two ways by the 09:11Z fire, so the evaluateTeams half is checked; demanding bit-identical HP margins was never achievable against this engine. The tournament.mjs half is its own ticket, T15c below. Do not re-open this box.]**
  - **`evaluateTeams` half DONE this fire (2026-08-21T09:11Z, see PROGRESS) — box stays UNCHECKED because the ticket's literal "identical rankings" bar doesn't hold in full generality; `scripts/tournament.mjs` half not started.** Spec-carrying plumbing landed in `src/scoring/index.js` (`builtMons[key].spec`, `buildMetaMon`/`buildRecommendedMon` return `.spec` too) and `evaluateTeams` gained `opts.threads` + the CLI a `--threads` flag, verified: win rates/team ranking ARE identical serial vs threaded (test/teams.test.js, plus a real CLI run on sampled data). BUT verification also caught a real, previously-undocumented pvpoke engine characteristic (src/engine/README.md's new "Known limitation" section): a reused Pokemon instance's `resetMoves()` bestChargedMove tie-break is sensitive to battle ORDER (stale `self.index` read in `initializeMove`), so exact `avgHpMargin`/`safeSwap.avgHpPct` can drift a little between serial and threaded runs even though win/loss can't (verified). This is pre-existing pvpoke behavior, not a T15b bug — but it means the ticket's literal bit-identical acceptance bar is unmet in general, so the box stays open per standing rule 3. See ROADMAP's "TrainingAI variance study" gap, which this is direct evidence for. `scripts/tournament.mjs`'s `--threads` wiring is UNSTARTED — split out as **T15c** below (same spec-carrying pattern needed there, applied to its own candidate/opponent member shapes; that file is large/sensitive per T13's "routine fires must not redo" note, so treat as its own bounded ticket).
- [x] **T15c — (seq after T15b's evaluateTeams half) Wire `opts.threads`/`--threads` into `scripts/tournament.mjs`.** **[Orchestrator adjudication 2026-08-21T10:15Z, answering the 09:56Z fire's two questions: (1) YES — checked. The achievable determinism bar for threaded mode is: win rates equal within the test's bounded tolerance, ranking order stable for clearly-separated teams; occasional single-battle win/loss reclassification under reordering is pvpoke's own `resetMoves()` order-sensitivity (README Known limitation), not a wiring bug, and the wiring itself is done + twice-verified (5/5 suite, 145/145 npm test, real acceptance run). (2) T15b's checked status STANDS — its evaluateTeams wiring is equally sound — but its README-era claim "win/loss outcomes are unaffected" is refined by this fire's larger-scale evidence: rare flips are possible on knife-edge battles. STANDING USAGE RULE from both findings: serial (no --threads) is the reference mode and remains bit-deterministic per seed; --threads is a statistically-equivalent speed mode — rankings trustworthy, exact per-battle results not reproducible across thread counts. Fine-grained final-stage rankings (like tournament stage 3) should prefer serial or a fixed thread count recorded in the report.]** Split out from T15b (see above) to keep each fire's diff bounded — `scripts/tournament.mjs`'s `runFunnelStage` drives `battleTeams` directly (not `evaluateTeams`) with its own `{key, speciesId, name, pokemon}` member shape (no `spec`), for the same reason evaluateTeams needed the fix: no raw `{ivs, shadow, bestBuddy}` to hand `runBattles`. Candidates there come from `matrix.builtMons` (already fixed, has `.spec` now) and opponents from `loadMetaTeams`/`sampleOpponentTeams` (also already fixed) — so the raw-param source data already exists after T15b; this ticket is "thread the existing `.spec` field through tournament.mjs's own member-building + battle loop," not rebuilding the plumbing again. Preserve tournament.mjs's per-battle skip-and-continue error handling (T13's explicit design choice — `runBattles` itself rejects the whole batch on one bad spec, so either wrap per-battle or batch per-candidate with a try/catch around each `runBattles` call). Document the same avgHpMargin-drift caveat T15b found. Verify: `node --test test/tournament.test.js` (serial vs `--threads` — same caveat: winRate/ranking identical, HP margin may drift) + `npm test` green + measured wall-time improvement on a real run recorded in PROGRESS.
  - **Wiring DONE this fire (2026-08-21T09:56Z, see PROGRESS) — box stays UNCHECKED, same reason T15b's was left open.** `runFunnelStage` now accepts `opts.threads`/`--threads` on the CLI, batching each CANDIDATE's entire battle set (every opponent × every pairing) into one `runBattles()` call (chose per-candidate over per-stage batching specifically because `runBattles` rejects its whole batch on one bad spec — the ticket's own text authorized this tradeoff). Verified: `node --test test/tournament.test.js` 5/5, `npm test` 145/145, real wall-time improvement measured (see PROGRESS: ~1.23x overall on a 792-battle run, up to ~1.8x on stage 3 alone where per-candidate batches are largest — smaller per-candidate batches in stages 1-2 pay worker-pool boot cost more times, diluting the win, a documented tradeoff of the per-candidate batching choice). **But writing this fire's own serial-vs-threaded test caught a real, previously-undocumented gap in T15b's claim**, not just a re-confirmation of it: at a larger battle volume than T15b's test covered (144 stage-3 battles vs T15b's 72), a real run hit a case where the SAME reused-instance `bestChargedMove` order-sensitivity (`src/engine/README.md`'s "Known limitation") reclassified one battle's verdict (a serial win became a threaded tie) — not only its HP margin, which is what T15b's (and the orchestrator's amended) bar assumed was the only thing that could drift. `test/tournament.test.js`'s new test therefore checks win rates within a small documented tolerance (3/36 battles' worth) rather than exact equality, and `src/engine/README.md` now carries a correction to T15b's stronger claim. This means T15b's own already-checked box rests on a claim ("win/loss outcomes... are unaffected") that this fire found is not strictly true in general — flagging that for the orchestrator to weigh, alongside whether T15c's bounded-tolerance win-rate match should be accepted the same way T15b's HP-margin drift was.
- [x] **T16 — (post-MVP, from ROADMAP known-gaps + PLAN.md stretch list; unrelated/parallel-safe to T13/T14/T15) HTML report.** `src/report/index.js`: new `renderReportHtml(input)` renders the same `ReportInput` `renderReport` already formats (settings, ranked teams with per-team win% tables/safe-swap/hardest-matchups, opponent meta team list, per-mon 1v1 score appendix, collection warnings) as a single self-contained HTML page — inline `<style>` only, no external CSS/JS/fonts/network refs (opens directly via `file://`), all interpolated report text (warnings, names, notes — sourced from user CSV/gamemaster data) HTML-escaped. Wired into `src/cli.js`: writes `out/report.html` alongside `out/report.md` by default; new `--html PATH` (default `out/report.html`) and `--no-html` flags. No engine/scoring/battle changes; pure formatting, mirrors the existing Markdown renderer's structure. Picked/numbered T16 (not T13) because T13 is reserved for the in-flight overnight-tournament-runner ticket noted in PROGRESS 06:52Z — this ticket does not touch that work. Verify: `node --test test/cli.test.js`.
- [x] **T13 — (session-built, orchestrator-verified; routine fires must NOT redo this) Multi-stage overnight tournament runner.** `scripts/tournament.mjs` + `test/tournament.test.js`: 3-stage funnel (defaults 500 candidates × 50 opponents → top 100 × 200 fresh opponents → top 10 × 500 opponents incl. the ENTIRE curated pool) ranking sampled candidate teams from a collection. Stages 1–2: 3 battles per matchup (each candidate lead × seeded-random opponent lead); stage 3: full 9 pairings + bestLead/safeSwap. Candidate always teamA (fixed-side offset cancels). Fresh opponent seed per stage; deadline self-tuning (`--deadline-minutes`, floors s2≥60/s3≥100, s3 up to 2× on surplus); per-battle skip-and-continue error handling; checkpoints `out/tournament-s{1,2,3}.json` with config-fingerprint resume + persisted deadline clock; report + `out/tournament-DONE` marker. No src/** changes; battleTeams driven directly (evaluateTeams untouched). Verified at check-off: `node --test test/tournament.test.js` 4/4 (worker + orchestrator re-run), `npm test` 123/123, smoke run exercising deadline-tuning both directions + curated-cap edge. Verify: `node --test test/tournament.test.js`.
- [x] **T17 — (post-MVP, from ROADMAP known-gaps + PLAN.md stretch list) Current-moves mode.** Rank using each mon's actual TM'd moveset (from the CSV's move columns) instead of always applying pvpoke's recommended moveset. Poke Genie exports carry `Quick Move`/`Charge Move`/`Charge Move 2` (human-readable move names); the generic format has no documented move columns but may opportunistically carry them too (mirror the Best Buddy/lucky opportunistic-column pattern). New `src/importer/moves.js`: a move-name-to-moveId resolver scoped to the CSV's resolved speciesId's own `fastMoves`/`chargedMoves` pool from the vendored gamemaster (not a global name index — avoids cross-species moveId collisions like Aegislash's `AEGISLASH_CHARGE_AIR_SLASH`), same normalization approach as `src/importer/gamemaster.js`'s species resolver. `NormalizedMon` gains an optional `moves?: {fastMove, chargedMoves}` field (resolved moveIds; present only when the CSV named a move AND it resolved against that species' own pool). `src/scoring/index.js`'s `scoreCollection` gains `opts.currentMoves` (default false = today's behavior unchanged): when true, a user mon with resolved `moves` gets `applyGroupMoveset` applied after `buildPokemon` (mirrors `buildMetaMon`'s own pattern — no new move-selection logic, reuses the existing exported primitive) and its `spec` gains `fastMove`/`chargedMoves` (so `--threads` parallel plumbing, which already reapplies `spec.fastMove` generically per T15b's `parallelWorker.js`, needs zero changes); a mon lacking resolvable move data while the mode is on falls back to pvpoke's recommended moveset with a `warnings` note (never a hard failure — this project's own history flags "reliability" as the exact reason this was deferred, so a silent/graceful fallback is required, not an error). Wire `--current-moves` into `src/cli.js` (forwarded to `scoreCollection`); Settings line in both `renderReport`/`renderReportHtml` (`src/report/index.js`) shows `currentMoves=on` when set. No battle math added anywhere — `applyGroupMoveset` already exists and is reused verbatim. Verify: `node --test test/importer.test.js test/scoring.test.js test/cli.test.js`.

## --cp 2500 / Ultra League (2026-08-21, pulled from ROADMAP's "genuinely multi-file" gap) — split into scoped sub-tickets so each fire's diff stays bounded
ROADMAP's gap note found CP 1500 hardcoded in engine/scoring/meta, not one place — too much for a single fire honestly. Split by layer, sequential; each sub-ticket's default (`cp` omitted/1500) must stay byte-identical to today's output.

- [x] **T18 — Engine layer.** `src/engine/harness.js`'s `initEngine` gains `opts.cp` (default 1500): loads `rankings-<cp>.json`, registers it under `gm.rankings.alloverall<cp>`, calls pvpoke's own `battle.setCP(cp)` (always, harmless at the 1500 default since no pokemon exist on the Battle yet), returns `ctx.cp`. `buildPokemon` needed no changes (already reads `battle.getCP()` dynamically). Unsupported `cp` throws clearly. See `src/engine/README.md`'s new "CP-cap parameterization" section for the full design note. Verified: `node --test test/engine.test.js` (26/26, new `initEngine({ cp })` describe block reproduces `rankings-2500.json` ratings exactly, same bit-for-bit pattern as the existing CP-1500 tests) + `npm test` 168/168.
- [x] **T18b — (seq after T18) Scoring + meta layer.** Thread `cp` through `src/scoring/index.js` (`defaultCp1500Ivs` → `defaultIVs.cp<cp>`; gamemaster also has a `cp2500l40` alt spread for some species — investigate which one `rankings-2500.json` actually assumes before picking, likely `cp2500` by analogy) and `src/meta/teams.js` + `src/meta/usage.js` (hardcoded `.../1500.json` paths → `.../${cp}.json`). Default unchanged. Verify: `node --test test/scoring.test.js test/metaTeams.test.js test/metaUsage.test.js`. **Done 2026-08-21:** `src/scoring/index.js`'s `defaultCp1500Ivs` → `defaultIvsForCp(ctx, lookupId)`, reads `defaultIVs.cp${ctx.cp}` off gamemaster (no new opts needed — `ctx.cp` already flows from `initEngine`, T18); confirmed `cp2500` (not `cp2500l40`) is correct by citing T18's own test, which already reproduced `rankings-2500.json` bit-for-bit using `cp2500`, so no new investigation was required. `src/meta/teams.js`'s `DEFAULT_TEAMS_FILE` and `src/meta/usage.js`'s `DEFAULT_TRAINING_FILE`/`DEFAULT_RANKINGS_FILE` are now `` `.../${ctx.cp}.json` `` functions of `ctx.cp` (vendor has `gobattleleague/{1500,2500,10000}.json` and `rankings-{500,1500,2500,10000}.json`, confirmed present). **Scope note, NOT done here (flagged for T18c):** `src/scoring/index.js`'s `loadMeta`/`src/meta/usage.js`'s `DEFAULT_GROUP_FILE` still default to `groups/great.json` regardless of `ctx.cp` — which meta-GROUP file represents "the current meta" for a non-Great-League cp (e.g. `groups/ultra.json` at cp 2500) is the same category of design decision as T18c's community-teams-file question, not a mechanical path substitution, so left untouched and documented in a code comment. `src/meta/sampleTeams.js` has its own separate `DEFAULT_RANKINGS_FILE` hardcode (used for its moveset pool, not mentioned in this ticket's text) — also left untouched, noted for T18c/CLI-wiring awareness. Verified: `node --test test/scoring.test.js test/metaTeams.test.js test/metaUsage.test.js` → 14+12+10 = 36/36. `npm test` → 168/168 green (byte-identical default-cp behavior). Manual sanity check with a real `initEngine({ cp: 2500 })` ctx (not part of the automated suite, ad hoc verification): `loadMetaTeams` built 27 teams from `2500.json` with members at ~2497 CP (confirms actual Ultra League builds, not silently still-1500), `loadUsageWeights` built weights for 842 species from `rankings-2500.json`/`2500.json` training data, `loadMeta` confirmed unchanged (still `great.json`, as documented above).
- [x] **T18c — (seq after T18b) CLI + report + E2E.** `--cp 1500|2500` flag on `src/cli.js` (default 1500) forwarded through T18b's plumbing; `settings.cp` in both report renderers; README section; decide + document how `--cp 2500` interacts with the Great-League-only community teams file (`data/meta-teams-community.json`). Verify: `npm test` + a real `node src/cli.js fixtures/sample-pokegenie.csv --cp 2500 ...` acceptance run recorded in PROGRESS.md. **Done 2026-08-21:** `--cp` on the CLI (validated against pvpoke's shipped caps 500/1500/2500/10000, throws before any work on anything else), forwarded to `initEngine({ cp })` so T18/T18b's `ctx.cp` plumbing carries it the rest of the way. New `src/util/leagues.js` answers the two questions T18b deferred as design decisions: the meta GROUP file follows the cap (`great`→1500, `ultra`→2500, `little`→500, `master`→10000) in BOTH `src/scoring/index.js`'s `loadMeta` and `src/meta/usage.js`, and each league gets a display name for the report headings. Also fixed in the same pass, since `--cp 2500` is wrong without them: `src/meta/sampleTeams.js`'s own hardcoded rankings path (flagged by T18b) now follows `ctx.cp`, `src/engine/teamBattle.js`'s per-match `new Battle()` now calls pvpoke's own `battle.setCP(ctx.cp)` (safe pre-Pokemon, same as `initEngine`), and a `data/meta-usage.json` snapshot is ignored (stderr note, vendored-rankings fallback) unless its recorded `cp` matches the run — `scripts/refresh-usage.mjs` only ever fetches Great League, so without this a refreshed snapshot would silently feed GL usage weights into an Ultra run. **Community-teams decision (the ticket's open question): `data/meta-teams-community.json` is Great-League-only by construction (Season 27 GL teams), so `loadMetaTeams`' `includeCommunity` now defaults to `ctx.cp === 1500`** — an Ultra run faces pvpoke's Ultra GBL presets + weighted-random Ultra compositions instead; `includeCommunity: true` still forces it. Reports: H1/title/opponent-section use the league name, settings line gains `cp=<n>` ONLY when it differs from 1500, so a default run's report stays byte-identical. Verified: `node --test test/scoring.test.js` 15/15 (new test: default group file follows ctx.cp, and cp-2500 mons build above the GL cap), `node --test test/cli.test.js` 12/12 (new: full cp-2500 pipeline + both renderers + community teams absent at 2500 + unsupported-cp rejection), `node --test test/e2e.test.js` 3/3 (new: cp-2500 report on disk), `npm test` **172/172 green**. Acceptance run: `node src/cli.js fixtures/sample-pokegenie.csv --cp 2500 --candidates 6 --opponents 3 --pool 12 --score-meta 8 --out out/report-ultra.md` → exit 0 in **27s**, report titled "Ultra League Team Report", `cp=2500` in Settings, opponents genuinely Ultra-tier (Kyurem/Palkia/Gliscor teams), 5 teams named.

## Sim throughput II — deterministic parallelism (2026-08-21, Jaxon: "make it faster so I can run a larger simulation") — **FENCE LIFTED 2026-08-21 ~16:05Z: T20–T22 (and T23–T25, T20b below) are routine-eligible now.** T19 landed session-side; the session's determinism-investigation findings are baked into T20/T20b's ticket text — start from them, do not re-derive. T22's MEASUREMENT half stays orchestrator-local (see ticket). Still do NOT pull the vendor-pin-bump ROADMAP gap while T19–T25/T20b are in flight — it would shift battle results under active perf baselines.
Why session-owned: every acceptance bar here is a perf number measured on Jaxon's 8-core Mac (4P+4E); the sandbox is ~2.4x slower (T14: 172 vs 73 ms/battle) with too few vCPUs to verify any of it. Design + interface contract: PLAN.md Rev 4. Context: the 225K-battle overnight (T13) ran SERIAL in 4h13m because threaded mode was only statistically equivalent (reused-instance `resetMoves()` order sensitivity, src/engine/README.md "Known limitation") and the worker pool re-boots per `runBattles()` call (per-candidate batches → only ~1.23x, T15c). Standing invariants: vendor stays read-only (rule 4); after T20 the doctrine becomes "same spec+seed → same result regardless of execution order or thread count," replacing "serial is the reference mode."

- [x] **T19 — Persistent battle executor + per-spec fault isolation.** `src/engine/parallel.js`: new `createExecutor({threads, continueOnError})` → `{ run(specs) → Promise<results in spec order>, close() }` — pool boots its N workers (each with its own vm engine context) ONCE and is reused across many `run()` calls. With `continueOnError: true`, each result slot is `{ok:true, value}` or `{ok:false, error:{message}}` so callers keep skip-and-continue semantics at any batch size (today `runBattles` rejects a whole batch on one bad spec, which is exactly why tournament.mjs had to batch per-candidate). `runBattles(specs, opts)` keeps its exact current signature and behavior as a thin create→run→close wrapper (zero churn for existing callers/tests). Record BEFORE numbers first: `node scripts/bench.mjs` serial baseline on this machine, pasted into the worker report (do NOT modify bench.mjs — T22 owns it). Verify: `node --test test/parallel.test.js` (extended: pool reused across ≥3 `run()` calls with results identical to serial; bad spec + continueOnError → that slot errors, rest of batch intact; bad spec without it → whole-batch reject as today; worker crash → clean error, no hang; threads 1 and 4) + `npm test` green. **Done 2026-08-21T16:00Z (session worker; orchestrator re-verified `npm test` 186/186 = 172 + 14 new).** Design notes for later tickets: lazy pool boot on first non-empty `run()`; `run()` calls internally SERIALIZED (each batch's bookkeeping independent; `close()` queued behind in-flight work); worker crash is always batch-fatal even under `continueOnError` (infra fault, not per-spec) with whole-pool teardown and transparent fresh-pool boot on the NEXT `run()` — only explicit `close()` is terminal; thread count fixed at boot (not per-batch clamped); only `vendorRoot` init opt carried (no caller threads `cp` today — deliberate). Baseline on the target Mac BEFORE pool reuse (bench.mjs unmodified): serial 78.11ms/battle; per-call-boot threading 50.18 / 36.24 / 40.14 ms/battle at threads 2 / 4 / 7 — degrading past 4 threads because each `runBattles` call re-pays pool boot, the exact cost T21's adoption amortizes away. Known minor: parallelWorker's per-worker build cache has no eviction (fine at current scales; noted for long-lived pools).
- [x] **T20 — (seq after T19) Land the PROVEN mechanism-1 determinism fix: canonical `.index`/`.battle` stamp on all 6 team members.** Findings (session investigation 2026-08-21, summary in PROGRESS ~16:05Z entry — trust these, don't re-derive): bench members never get `setBattle`/`.index` stamped for the current battle (leads DO, via `setNewPokemon` at `src/engine/teamBattle.js:247-248`), so their `fullReset()`→`resetMoves()`→`initializeMove()` (`vendor .../pokemon/Pokemon.js:831-839`) reads the instance's PRIVATE stale `battle` ref — frequently the shared `ctx.battle` dirtied by unrelated 1v1 scoring earlier in the same pipeline (`harness.js:126` builds every mon with `index=0, battle=ctx.battle`) — and the `bestChargedMove` DPE tie-break can flip with whatever opponent that stale battle last held (proven by direct execution: same azumarill instance picks PLAY_ROUGH vs ICE_BEAM depending on a leftover opponent; 3 of 8 tested species flip; a never-3v3-battled mon picked up an unrelated mon from someone else's scoring battle as its tie-break opponent). THE FIX (proven zero-cost, and zero delta on a 360-battle serial sample): in `src/engine/teamBattle.js`, immediately BEFORE the existing all-6 `fullReset()` loop (~line 313): `for (const mon of orderedA) { mon.setBattle(battle); mon.index = 0; }` / `for (const mon of orderedB) { mon.setBattle(battle); mon.index = 1; }` — only pvpoke's own public `setBattle()` plus the identical `.index` assignment `Battle#setNewPokemon` itself performs (`Battle.js:82-120`). Do NOT rebuild-per-battle or clone instead (investigated: strictly dominated — fresh builds carry the same wrong-constant `index=0/ctx.battle` state and would need this stamp anyway). **KNOWN AND ACCEPTED: this does NOT make serial↔threaded bit-identical — a second, distinct mechanism exists (see T20b); do NOT write a battle-outcome-identity acceptance test, it is known-red today.** Acceptance: (a) new test asserting PRE-BATTLE team state bit-identity — each member's `index`, `bestChargedMove`, per-move damage/DPE — across canonical/reversed/shuffled orderings of the same battle set (this WILL pass; proven with instrumentation); (b) re-run `scripts/variance-study.mjs` with the fix and record the flip rate in PROGRESS (expect ≈unchanged ~0.28% — that residue is mechanism 2, not a failure of this fix); (c) if any existing test's expected values shift, verify the new value matches a FRESH-context build before updating it and say so in PROGRESS (session sample showed 0/360 delta, so expect no shifts). Verify: `node --test test/teamBattle.test.js test/parallel.test.js` + the new ordering test + `npm test` green.
  - **Done + verified 2026-08-21 (routine fire, see PROGRESS).** Fix landed exactly as specified. New test in `test/teamBattle.test.js` ("T20" describe block) dirties `ctx.battle` with real 1v1 sims against 5 different species in canonical/reversed/shuffled/none orderings, then builds a fresh probe team and asserts pre-battle `.index`/`bestChargedMove`/per-move `damage`/`dpe` are bit-identical across all of them — confirmed it actually catches the bug (fails with the fix reverted: move damage/dpe differ; passes with it). `scripts/variance-study.mjs --candidates 5 --opponents 8 --shuffles 3 --seed variance-study` re-run before/after the fix on this exact repo state was BIT-IDENTICAL (1/4 orderings flip 1/360, mean 0.069%) — not the ~0.28%-across-all-4 the ticket expected, because that figure had already drifted from unrelated repo changes since the original 10:23Z measurement (see ROADMAP.md's updated entry); the fix itself changed nothing for this seed/pool, consistent with mechanism 1 depending on unrelated leftover scoring-battle state rather than on battle order within the study. No existing test's expected values shifted. `npm test` → 187/187 green (186 prior + 1 new).
- [x] **T21 — (seq after T20) Tournament + evaluator adopt the persistent pool; deterministic partitioning; threaded by default for big runs.** `scripts/tournament.mjs`: ONE executor for the whole run, per-STAGE batching via `continueOnError` (preserving T13's per-battle skip-and-continue design), `--threads` default flips to `max(1, os.cpus().length - 1)` (serial via `--threads 1`), report + checkpoints record the thread count. `src/teams/index.js` (`evaluateTeams`): public interface stays FROZEN; internals may accept/reuse an executor across candidates when `opts.threads` is set (kill the per-call pool boot). **NEW REQUIREMENT (from the determinism findings): `createExecutor` gains a DETERMINISTIC spec→worker partition mode** (contiguous chunks or striped by spec index — pick one, document why) instead of availability-based dispatch, default ON for tournament/evolve-scale runs: each worker's battle-history sequence becomes a pure function of (specs, threads), so any threaded run is REPRODUCIBLE bit-for-bit for a fixed (seed, threads) — even though serial↔threaded may still diverge on ~0.28% knife-edge battles until T20b resolves mechanism 2. This extends T21's ownership into `src/engine/parallel.js` + `test/parallel.test.js` (T19 is landed; sequential ownership is fine). Tests: same-config threaded run repeats bit-identically (run twice, compare everything); serial-vs-threaded win rates within the existing documented tolerance (NOT exact — that bar is T20b's, don't assert it); threads recorded in report + checkpoint fingerprint. CLI default stays serial (small runs). Verify: `node --test test/tournament.test.js test/teams.test.js test/parallel.test.js` + `npm test` green.
  - **Partitioning half DONE 2026-08-21T18:34Z (see PROGRESS).** `src/engine/parallel.js`'s `createExecutor` dispatches via `partitionContiguous(n, workers)` (deterministic contiguous chunks) instead of availability-based dispatch. `runBattles`' signature/behavior/return-shape unchanged.
  - **`scripts/tournament.mjs` half DONE this fire (2026-08-21, routine — see PROGRESS) — box now checked.** `runTournament` boots ONE `createExecutor({ threads, vendorRoot, continueOnError: true })` (when `opts.threads` is set) before stage 1, reused across all 3 stages, closed in a `finally` (an inner `runStages()` closure lets `try/finally` wrap the whole stage-running body without hand-reindenting ~250 lines — closes over `executor`/`ctx`/etc.). `runFunnelStage` (`executor` param replaces `threads`) now builds EVERY candidate's specs for the WHOLE STAGE into one flat array and submits it as a single `executor.run()` call (continueOnError), instead of T15c's one-`runBattles()`-call-per-candidate — a `{ok:false}` result slot fails only that one battle (logged exactly like the serial path's per-battle skip-and-continue), not a whole candidate's batch as before; a genuine worker CRASH (not a bad spec — `continueOnError` doesn't catch those, per src/engine/parallel.js's documented crash policy) still fails every spec in the stage, accepted as a rare-infra-fault tradeoff since the persistent executor transparently reboots on the next stage's `run()` call. `--threads` on `scripts/tournament.mjs`'s own CLI now defaults to `defaultThreadCount()` (`max(1, cpus-1)`, imported from `src/engine/parallel.js`) — `--threads 1` restores serial. **Judgment call: `runTournament`'s own programmatic default (`opts.threads` omitted) stays serial, unchanged** — only `main()`'s CLI-flag resolution was flipped. Read "CLI default stays serial (small runs)" in this ticket's own text as referring to `src/cli.js`'s separate `--threads` flag (untouched, always was serial-default, out of this ticket's scope), not to `scripts/tournament.mjs`'s own programmatic API — flipping `runTournament`'s own default would have silently made every existing caller (`test/tournament.test.js`'s determinism/resume/report tests, none of which pass `threads`) run threaded by default, which risked flaking the exact-equality determinism assertions on this engine's known ~0.28% knife-edge residue (src/engine/README.md's "Known limitation") for no requirement in the ticket text. Report + checkpoints record the thread count: new `threadsUsed` field on each stage object (survives to checkpoint JSON; deliberately NOT part of `buildRunConfig`'s fingerprint, matching the ticket's own "NOT part of the checkpoint config fingerprint" framing for the pre-existing `threads` opt) and a new Settings-line in the Markdown report (`- threads: stage1=N (worker-pool executor)|serial, ...` per stage, since a resumed stage can legitimately show a different value than a freshly-run one). **`evaluateTeams` half deliberately NOT done — same judgment call the partitioning-fire already recorded, re-confirmed rather than silently dropped:** `evaluateTeams` already makes exactly one `runBattles()` call per invocation, so a single call's pool-boot cost is already amortized across every candidate within that call; a persistent executor would only help across MULTIPLE `evaluateTeams` calls over time, and nothing calls it more than once yet (T24's GA driver will be the first — `evaluateTeams`'s public interface is already frozen per PLAN Rev 3/4, so accepting an external executor is a pure addition when that caller exists). Verify commands run clean THIS fire: `node --test test/tournament.test.js` → 5/5 (extended the existing threaded-vs-serial test with `threadsUsed`/report-Settings-line assertions rather than adding a whole new test, since the fault-isolation mechanism itself is `continueOnError`, already unit-tested at its source in `test/parallel.test.js` per T19 — this fire only wires it through). `npm test` → **193/193 green** (same count as baseline; assertions were added inside an existing test, not a new one). Real CLI acceptance run (`node scripts/tournament.mjs fixtures/sample-pokegenie.csv --score-meta 4 --pool 8 --s1-candidates 5 --s1-opponents 3 --s2-top 3 --s2-opponents 3 --s3-top 2 --s3-opponents 3 --seed t21-accept --deadline-minutes 5`, no `--threads` flag i.e. the new CLI default): exit 0, 14.0s wall, report's Settings line correctly reads `threads: stage1=3 (worker-pool executor), stage2=3 (worker-pool executor), stage3=3 (worker-pool executor)` (3 = max(1,4-1) on this sandbox's 4 vCPUs), 0 errors across 180 battles; the identical config with `--threads 1` (serial) took 26.3s wall and named the same top team (Lanturn/Sableye/Azumarill) — a real, if sandbox-scale, speedup confirming the wiring works end to end, not just in tests. Full correctness/perf measurement at tournament scale is still T22's job on Jaxon's Mac.
- [x] **T22 — (seq after T21) Measured speedup on target hardware + docs.** `scripts/bench.mjs` gains a threaded mode (`--threads N`) reusing the executor; `test/bench.test.js` covers it at tiny N. On THIS Mac, record in PROGRESS.md: serial vs threads∈{4,6,8} ms/battle bench table, plus a real mid-size tournament A/B (same seed, same config, ~2–5K battles): wall time serial vs threaded-default, identical rankings asserted, achieved speedup (gate: ≥3x; target 4–6x). README + src/engine/README.md: new threads guidance (threaded is now deterministic and the default for tournament-scale runs; what changed vs the old serial doctrine). ROADMAP: note the initiative done. Verify: `node --test test/bench.test.js` + `npm test` green + the A/B numbers pasted in PROGRESS.md. **Split ownership per Jaxon's routines-first directive: the code half (bench --threads, tests, docs) is routine-eligible once the fence lifts; the measurement half (the actual ms/battle + A/B numbers) can ONLY come from Jaxon's Mac — a fire must NOT fabricate or sandbox-substitute those numbers; leave the PROGRESS measurement entry to the local orchestrator and say so in your fire's entry.** **Done 2026-08-21T19:43Z — code half by the 19:14Z fire (9dbc332: bench `--threads`/`--batches` persistent-pool mode + docs, its suites green), measurement half by the orchestrator on the target Mac (PROGRESS 19:43Z entry): bench n=150 serial 72.7 / t4 39.2 / t6 36.7 / t8 51.4 ms/battle (t8 regresses — E-core contention; 6–7 threads is the sweet spot); tournament A/B, 3,225 battles, same seed/config: wall 217s → 82s = 2.65x overall, per-battle at production batch sizes 73.7 → 22.9 ms (stage 3) = 3.2x sustained; ≥3x gate MET at sustained/large-batch rates, overall 2.65x on this small A/B due to fixed setup + small stage-1 batches; the 4–6x aspiration was NOT reached on this 4P+4E chip. Top-2 finalists + all shared teams' win rates identical serial-vs-threaded; ONE cut-line finalist swap observed — recorded as funnel-scale mechanism-2 evidence on T20b.**
  - **Code half DONE this fire (2026-08-21, routine — see PROGRESS) — box stays UNCHECKED, measurement half not attempted (correctly, per the ticket's own split-ownership note).** `scripts/bench.mjs` already had a one-shot `--threads N` mode (built earlier, GOALS T15, via `runBattles()`); what was actually still missing — flagged explicitly in `src/engine/README.md`'s Performance section ("that instrumentation is GOALS T22's job... not yet benchmarked end-to-end") — was a mode exercising the T19/T21 *persistent*-pool amortization (many `run()` calls against ONE pool, not one pool per invocation). Added `runBenchPersistent` + `--batches B` (B>1): splits N battles into B groups run via repeated `executor.run()` calls against a single `createExecutor()` pool, printing per-batch ms/battle so batch 0 (boot+battles) is directly comparable against batch 1+ (amortized, no boot); `--threads` alone (or `--batches 1`) is unchanged, byte-for-byte the pre-existing one-shot behavior. `test/bench.test.js` +3 tests (well-formed multi-batch report at tiny N; batch-split turn counts match serial `runBench` exactly at 1 batch and 3 batches; executor closes cleanly, no lingering worker handle). README.md gained a `--threads` line in the CLI Options block (previously undocumented despite existing since T15b) and a new "Performance: parallel battles (`--threads`)" section (CLI + tournament.mjs usage, the reproducible-at-fixed-(seed,threads) doctrine, serial-stays-reference-for-knife-edge-battles caveat, pointer to bench.mjs); `src/engine/README.md`'s Performance section documents the new `--batches` mode inline, explicitly repeating that the actual ms/battle numbers are the measurement half and must come from Jaxon's Mac, not a sandbox run. Verified: `node --test test/bench.test.js` → 7/7 (4 prior + 3 new). `npm test` → **196/196 green** (193 prior + 3 new). Manual sanity run: `node scripts/bench.mjs --n 12 --threads 2 --batches 3` printed a clear boot-vs-amortized per-batch breakdown (values not recorded in PROGRESS — sandbox-scale, N=12 is far too small/noisy to mean anything, and per the ticket's own rule sandbox numbers must never substitute for the real measurement). **What's left for T22:** the actual local A/B (serial vs threads∈{4,6,8} bench table + a real ~2-5K-battle tournament A/B with wall time + speedup gate) — orchestrator/Jaxon-local only, per the ticket's split-ownership note; a routine fire must not attempt this.

## Evolutionary team search — "survival of the fittest" (2026-08-21, Jaxon's directive) — routine-eligible AFTER the T19–T22 fence lifts (T23 is parallel-safe with T20–T22; T24 needs T21's executor adoption landed)
Jaxon's ask, verbatim spirit: gen 0 = random candidate teams from his collection; winningest teams move on, some survivors mutate (one Pokemon swapped), losiest teams die; repeat until the best teams/cores converge — compute concentrates on good teams instead of being spread evenly. Plus, if cheap, per-generation data on specific Pokemon (e.g. representation per generation — it IS cheap, it's just counting, so it's in scope by default). Full design + interfaces + selection-scheme defaults: **PLAN.md Rev 5** — read it before starting any T23–T25 ticket. GA code is sampling machinery, not battle math (standing rule 4 untouched). Everything seeded/deterministic; battles go through the Rev 4 executor.

- [x] **T23 — (parallel-safe with T20–T22; no engine files touched) GA core module.** New `src/teams/evolve.js` per PLAN.md Rev 5 interfaces: `initPopulation` (delegates to `sampleCandidateTeams`), `nextGeneration` (selection per PLAN Rev 5 **as revised 2026-08-21 by Jaxon**: only the bottom `deathRate` fraction dies, default 0.25; each SURVIVOR rolls a seeded mutation chance scaling with fitness percentile — linear `mutationFloor` 0.05 → `mutationCeil` 0.40, tunable — a success spawning one mutant with ONE uniform-random slot swapped, swap-in weighted by the Rev 3 blend; population held at exactly P with mutants filling dead slots, oversubscription resolved toward higher-percentile parents, and a ~10%-of-P immigrant floor always reserved; dedupe by species-set; no-dup-species rule; bounded resample retries; returns `{population, lineage}`), `hasConverged` (top-10 set stable 3 gens). Pure functions, NO battles inside, fully deterministic under seed. Unit tests use FAKE fitness arrays (no engine init, keeps the suite fast): determinism under seed; deathRate honored exactly; mutation probability monotone in fitness percentile (statistical check over many seeded generations of fake fitness); mutants differ from parent in exactly one slot and never duplicate a species; population stays unique + exactly at target size with the immigrant floor respected; convergence detector fires exactly when specified; excludeSpecies honored end to end. Verify: `node --test test/evolve.test.js`.
  - **Done + verified 2026-08-21 (routine fire, see PROGRESS).** `src/teams/evolve.js` landed exactly per the Rev-5-as-revised interfaces. `src/teams/sample.js` gained 3 small additive exports (`DEFAULT_BLEND_ALPHA`, `buildScoredPool`, `makeBlendedWeightFn` — same code, no behavior change) so evolve.js's mutant swap-in reuses the exact Rev 3 blend instead of re-deriving it. `test/evolve.test.js`: 10/10 (all ticket-specified checks, run 3x clean to rule out flake in the statistical mutation-monotonicity test — see PROGRESS for the one fragile-banding fix made mid-fire). `node --test test/sampleCandidates.test.js test/evolve.test.js` → 20/20 (sample.js's own suite unaffected by the additive exports). `npm test` verification recorded in PROGRESS.
- [x] **T24 — (seq after T21 + T23) Evolution driver script.** New `scripts/evolve.mjs`, sibling of tournament.mjs (which stays): collection→matrix setup mirroring tournament.mjs's existing path; flags `--population` (default 100), `--opponents-per-gen` (default 20), `--generations` (default 15), `--seed`, `--threads` (default = T21's default), `--deadline-minutes`, `--cp` (via ctx like everything else), `--fixed-opponents`, `--out`. Per generation: fresh seeded opponent draw (`sampleOpponentTeams`), evaluate EVERY team (elites included — no stale fitness) vs all M opponents at 3 seeded lead pairings via ONE persistent executor with `continueOnError` per-battle skip-and-continue; then T23's `nextGeneration`. Checkpoint per generation (`out/evolve-gen<N>.json` or single rolling file — config-fingerprint resume like tournament.mjs); analytics collected per gen per PLAN Rev 5 (representation, species mean fitness, survival rate, origin counts, elite pair/cores) into `out/evolve-generations.json`. Final gen elites get full 9-pairing evaluation + bestLead/safeSwap. Budget math documented in the script header (PLAN Rev 5 has the example). Verify: `node --test test/evolveScript.test.js` (tiny: pop 6, 2 gens, 2 opponents, deterministic under seed, checkpoint resume works, analytics JSON shape) + `npm test` green.
  - **Done 2026-08-21 (routine fire, see PROGRESS) — box checked.** `scripts/evolve.mjs` landed against the ALREADY-SHIPPED T23 API (a parallel fire landed `src/teams/evolve.js` first — this fire built directly against its real `nextGeneration` return shape: `{population, lineage: {died, entries: [{origin, parentIndex?, swappedSlot?}]}}`, not a guessed one). Per-generation checkpoints (`out/evolve-gen<N>.json`, containing that generation's population/fitness/lineage-to-next AND `nextPopulation`, so resume never has to recompute `nextGeneration` — it just reads the stored offspring) + a rolling `out/evolve-generations.json` (analytics only, capped at 25 species/gen, documented). **Two judgment calls not pinned down by the ticket text, documented in the script's header + here:** (1) `--elites` count (PLAN Rev 5 never specifies how many final-gen teams get the full 9-pairing pass) — added as its own flag, default 10, mirroring tournament.mjs's `s3Top` default; (2) `--deadline-minutes` is a simple stop-before-the-next-generation check, NOT tournament.mjs's opponent-count self-tuning — Rev 5's flag list never asks for tuning here and evolve has no "narrowing funnel" structure to tune against, so a flat stop is the honest simpler choice. GA tunables (deathRate/mutationFloor/mutationCeil/immigrantFraction/alpha, convergence window/topN) are NOT exposed as CLI flags — not in the ticket's flag list; `src/teams/evolve.js`'s own DEFAULT_* constants are used as-is.
- [x] **T25 — (seq after T24) Evolution report + README.** Report output for evolve runs (md + html via the existing renderers' patterns): final ranked elite teams (win%, bestLead, safeSwap), convergence summary (generations run, stop reason, mean/max fitness per gen), species-trajectory table (top ~15 species: representation per generation), top cores (elite 2-species pairs) — reading `out/evolve-generations.json`, no new battles. README section: what the mode is, the selection scheme in plain words, flags, reproducibility via seed, and that analytics are free. Verify: `node --test test/evolveScript.test.js` + `npm test` green + an acceptance run `node scripts/evolve.mjs fixtures/sample-pokegenie.csv --population 8 --opponents-per-gen 3 --generations 3 --seed accept-t25` (exit 0, report written, trajectory table present) recorded in PROGRESS.md.
  - **Done 2026-08-21 (routine fire, see PROGRESS).** The Markdown report (final ranked elites, convergence summary, generation-by-generation table, species trajectory, top cores) already existed from T24's own `renderEvolveReport` -- this fire's actual remaining scope was the missing **HTML** twin (`renderEvolveReportHtml`, same section order/content as the Markdown, mirrors `src/report/index.js`'s `renderReportHtml` pattern including its inline-only CSS + `escapeHtml` on every interpolated user/gamemaster string), wired into `runEvolution` (writes `out/my-teams-evolve.html` by default, new `--html PATH`/`--no-html` CLI flags + `opts.html`/`opts.noHtml`, `result.htmlPath`), plus the README section. No changes to the GA core, battle driving, or the Markdown renderer's content.
- [x] **T26 — (parallel-safe; data + tests only, no engine/script changes) Community teams: add the Yasser Aleed set (13 Great League teams).** Source: stream screenshots from GBL streamer Yasser Aleed (twitch.tv/YasserAleed, YouTube aleed_yasser, Discord /thecoolkidz) supplied by Jaxon 2026-08-21. **The orchestrator's transcription below is the ONLY machine-readable record — fires cannot see the images; treat this ticket text as the source data.** League: Great League assumed (species profile is squarely GL-meta; the community file is GL-only per T18c). Extend `data/meta-teams-community.json` following its existing schema + provenance conventions (T10b): ids `community:yasseraleed-N`, untagged (meta) tier, provenance note naming the source + "visual transcription from stream, 2026-08-21". The thirteen teams (15 screenshots total; where an on-screen team-builder card was visible its text is authoritative over my visual reads; shadow status listed ONLY where the stream showed it):
  1. Lickilicky / Corviknight / Quagsire — Corviknight CONFIRMED by Jaxon directly (2026-08-21); no uncertain flag needed.
  2. Tinkaton / Quagsire / Corsola (Galarian) — resolve Galarian Corsola via the gamemaster's actual id (likely `corsola_galarian`).
  3. Altaria / Swampert / Tinkaton — shown three separate times; clearly a favored core.
  4. Quagsire / Empoleon / Feraligatr.
  5. Mimikyu / Ninetales / Jumpluff — team card (seen twice): Mimikyu (Shadow Claw, Shadow Sneak, Play Rough), Ninetales (Ember, Weather Ball (Fire), Energy Ball), Jumpluff (Fairy Wind, Energy Ball, third move cut off).
  6. Lickilicky / Tinkaton / Mimikyu — team card: Lickilicky (Rollout, Shadow Ball, Body Slam), Tinkaton (Fairy Wind, Gigaton Hammer, Bulldoze), Mimikyu (Shadow Claw, Shadow …cut off). Lickilicky render was shiny (yellow) — cosmetic only.
  7. Dragonite (Shadow) / Diggersby / Tinkaton — Dragonite rendered with a shadow aura (and shiny-green — cosmetic only).
  8. Tinkaton / Diggersby / Feraligatr.
  9. Wigglytuff / Malamar / Jellicent.
  10. Sliggoo / Feraligatr (Shadow) / Sableye (Shadow) — CORRECTED by Jaxon directly (2026-08-21): the on-screen card listing Empoleon was stale; the panel's Shadow Feraligatr render was the real member. Sliggoo moves from the card: Dragon Breath, Dragon Pulse, Body Slam; Sableye (Shadow) moves cut off; Feraligatr's likely set is the ranking list's (Shadow Claw, Hydro Cannon, Ice Beam) — note as such, don't assert.
  11. Corsola (Galarian) / Tinkaton / Kingdra — team card: Corsola-G (Astonish, Power Gem, Night Shade), Tinkaton (Fairy Wind, Gigaton Hammer, Bulldoze), third member Kingdra by visual (blue seahorse).
  12. Malamar / Feraligatr / Tinkaton.
  13. Stunfisk / Malamar / Wigglytuff — NORMAL Stunfisk (orange with yellow lips), explicitly NOT Galarian.
  Supplementary stream data for entry NOTES (not required to be wired): the stream's own top-14 mon ranking with movesets — 1 Lickilicky (Rollout, Body Slam, Shadow Ball), 2 Tinkaton (Fairy Wind, Gigaton Hammer, Bulldoze), 3 Altaria & 6 Altaria-Shadow (Dragon Breath, Sky Attack, Flamethrower), 4 Empoleon & 7 Empoleon-Shadow (Metal Sound — confirmed by two on-screen team cards; Hydro Cannon, Drill Peck), 8 Quagsire-Shadow & 9 Quagsire (Mud Shot, Aqua Tail, Mud Bomb), 10 Jellicent (Hex, Surf, Shadow Ball), 11 Forretress (Volt Switch, Sand Tomb, Rock Tomb), 12 Ninetales & 13 Ninetales-Shadow (Ember, Weather Ball (Fire), Energy Ball), 14 Feraligatr (Shadow Claw, Hydro Cannon, Ice Beam). Later in the same stream the ranking was reshuffled with Mimikyu promoted to #1 (then: 2 Lickilicky, 3 Tinkaton, 4 Altaria, 5 Empoleon, 6 Altaria-Shadow, 7 Empoleon-Shadow, 8 Quagsire-Shadow, 9 Quagsire, 10 Jellicent, 11 Forretress, 12 Ninetales, 13 Ninetales-Shadow; Feraligatr no longer visible) — note both orderings, don't reconcile them. Team entries default to NON-shadow variants (the screenshots don't specify shadow status per team); mention shadow alternatives in notes. Movesets: if the file format already carries per-member movesets, include the captured ones; if members are bare speciesIds, either record moves in notes only OR minimally extend the loader to honor an optional per-member moveset via the existing `applyGroupMoveset` primitive — judgment call, document which in PROGRESS. Gen-9/possibly-unreleased species (Tinkaton, Mimikyu) may be absent from the pinned gamemaster — the T10b loader convention already covers this (skip-with-stderr-warning; drop a team whole if ANY member fails); do NOT hack around a missing species. Update `test/metaTeams.test.js` expectations for the grown file (keep ≥-style count assertions robust to dropped teams). Verify: `node --test test/metaTeams.test.js` + `npm test` green + a PROGRESS note listing which of the 13 teams resolved fully vs dropped under the pinned gamemaster.
  - **Done 2026-08-21 (routine fire, see PROGRESS).** All 13 teams added to `data/meta-teams-community.json` verbatim per this ticket's own transcription (raw ids `yasseraleed-1`..`yasseraleed-13`, matching the file's existing unprefixed-id convention — the loader itself prepends `community:`), untagged (meta) tier, non-shadow variants by default except where the ticket named an explicit shadow render (team 7's Dragonite, team 10's Feraligatr/Sableye — shiny/cosmetic renders in teams 6 and 7 explicitly NOT treated as shadow). Every named speciesId (`corviknight`, `corsola_galarian`, `tinkaton`, `mimikyu`, `jumpluff`, `dragonite_shadow`, `diggersby`, `feraligatr`/`feraligatr_shadow`, `sliggoo`, `sableye_shadow`, `kingdra`, `malamar`, `wigglytuff`, `jellicent`, `stunfisk` (normal, confirmed distinct from `stunfisk_galarian`)) checked present in the pinned gamemaster BEFORE writing the data (none of the "gen-9/possibly-unreleased" concern applied here — all 13 resolved). **All 13/13 teams resolved fully battle-ready** under `loadCommunityTeams` (verified directly, not just via the test suite). **Movesets judgment call:** recorded in each team's `notes` field only (the ticket's first option) — the file format has never carried structured per-member movesets for any of its existing 33 entries, and extending the loader/schema to honor an optional per-member moveset via `applyGroupMoveset` for 2-3 partially-known movesets (several are explicitly "cut off" in the source screenshots) would be a real schema change for marginal benefit; every member instead gets pvpoke's recommended moveset via the existing fallback, same as every other community team. The stream's supplementary top-14 ranking data was NOT wired in anywhere (ticket marked it optional/not-required). Test file's hardcoded counts updated (33→46 total entries, floor 26→39 resolved, both confirmed against the actual loader output before writing, not guessed).

## Determinism, mechanism 2 (2026-08-21, from the session determinism investigation) — deliberately queued AFTER the evolution tickets: T23–T25 deliver user-facing value; this is a correctness refinement
- [x] **T20b — (seq after T22; do T23–T25 first per queue order) Hunt the residual order-dependence inside battle execution.** Evidence (session investigation, PROGRESS ~16:05Z): with mechanism 1 fully neutralized — pre-battle state instrumented and proven bit-identical between orderings for all 6 mons (index, bestChargedMove, per-move DPE) — the reproduced flip battle `4|4|2|2` of the variance-study config (5 candidates × 8 opponents × 9 pairings, canonical vs reversed ordering) STILL flips winner a↔b with an identically-derived seed. So a second, distinct mechanism acts DURING the turn loop. FUNNEL-SCALE EVIDENCE (T22 A/B, 2026-08-21): at 3,225 same-seed battles serial vs threaded-7, top-2 finalists and every shared team's win rate were identical, but one knife-edge flip near the stage-2 top-5 cut swapped which team advanced to the final — the residual CAN move a cut-line boundary at funnel scale, not just an HP margin. Weigh this when prioritizing. Strongest lead: `vendor/pvpoke/src/js/training/TrainingAI.js:815-957` `runScenario` — called at battle start (`evaluateMatchup`), on switch-timer expiry, and on BENCH members for counter-switch evaluation (`:664-682`) — builds a throwaway Battle and calls `setNewPokemon` on real team mons (mutating `.battle`/`.index`/`baitShields`/`farmEnergy`/`priority`), and its restore block (`:921-937`) restores 7 fields but NOT `.battle`, `baitShields`, or `priority` (no un-`setBattle` exists). Not yet proven to be THE cause — instrument before fixing. Permitted fix shape: WRAP vendor functions from our harness (save the missing fields before, restore after, via vendor's own setters) — driving/wrapping pvpoke's code is allowed; editing vendor files or reimplementing battle logic is NOT. If a full fix lands: re-run `scripts/variance-study.mjs` (expect flip rate → 0/360), THEN upgrade T20/T21's tests to full battle-outcome bit-identity across orderings AND thread counts, retire the serial-reference doctrine (engine README + PLAN Rev 4 amendment note). If not fully resolved after honest effort: document exactly what was ruled in/out in PROGRESS + engine README's Known limitation section and leave the middle doctrine standing — a documented residual beats a false fix. Verify: `node --test test/teamBattle.test.js test/parallel.test.js` + variance-study numbers in PROGRESS + `npm test` green.
  - **Partial finding, routine fire 2026-08-21T21:xxZ (see PROGRESS) — box stays UNCHECKED, real progress but not a full resolution.** Confirmed by direct code reading (not just instrumentation) that runScenario's restore block genuinely omits `.battle`/`.baitShields`/`.farmEnergy`/`.priority`, and traced every ACTUAL call site reachable from our headless battle path (`evaluateMatchup`, `decideSwitch`, `decideShield`, `runBulkScenarios` — the `generateTeam`/`testPool`/`calculateAverageRosterPerformance` call sites are dead code for us, never invoked) to confirm none of them depend on that leaked mutation persisting (each either re-sets the field itself right after, or only ever reads the scenario's *return value*). Landed `wrapRunScenario` in `src/engine/teamBattle.js` (exported, applied to both players' AI instances right after they're constructed): wraps `runScenario` so every call snapshots + restores exactly those 4 fields on both its `pokemon` and `opponent` arguments via pvpoke's own public `getBattle()`/`setBattle()` (a getter DOES exist, `Pokemon.js:1975`, so no vendor edit was needed to read the private `battle` ref) — a pure wrap, no vendor files touched, no battle math reimplemented. **Proven a real, non-no-op fix, but NOT sufficient to close this ticket:** re-running `scripts/variance-study.mjs --candidates 5 --opponents 8 --shuffles 3 --seed variance-study` with the fix applied shows the CANONICAL (single fixed order) win rate for one candidate shift (0.583→0.597) — proof the leak was live and this wrap changes real outcomes — but the SPECIFIC known 1-in-360 reversed-ordering flip this ticket's evidence names is **still present, unchanged** (still exactly 1/360 in the reversed ordering only, still 0.069% mean, still no ranking change). So a **third, still-unidentified mechanism** causes at least this specific flip — the `.battle`/`baitShields`/`farmEnergy`/`priority` leak this fire found and fixed is real but was not (or not solely) the cause of the flip named in this ticket's own reproduction. Per the ticket's own "not fully resolved" branch: leaving the box unchecked, the serial-reference doctrine standing, and NOT upgrading T20/T21's tests to bit-identity. Verified: `node --test test/teamBattle.test.js` → 11/11 (8 prior + 3 new "T20b" tests, isolated against fake stand-ins proving the wrap mechanism itself — restores all 4 fields on both args, restores even if the wrapped call throws, restores correctly across sequential calls). `npm test` → 217/217 green (214 prior + 3 new). A future fire continuing this hunt should NOT re-derive the `.battle`/baitShields/farmEnergy/priority leak (now fixed) — start instead from: what else differs turn-by-turn between a canonical and reversed run of the SAME battle set with THIS fix already applied? (candidates: TrainingAI's own per-instance `scenarios`/`currentStrategy`/`previousStrategy` state persisting across a mon's MULTIPLE evaluateMatchup calls within one battle in an order-sensitive way; DecisionOption/chooseOption's random draw consuming the shared seeded RNG stream in an order that itself depends on how many scenarios were evaluated before it, i.e. an accounting difference rather than a state-leak difference).
  - **Root cause found + fixed, routine fire 2026-08-22 (see PROGRESS) — box now CHECKED.** Concurrent with the fire above landing `wrapRunScenario` and reproducing the SAME "not the cause" conclusion, this fire independently found the actual third mechanism by instrumenting ALL SIX roster members' pre-battle state (not just the two active leads T20 already covers): pvpoke's own `fullReset()`/`Player#setRoster()` never initialize a Pokemon's `baitShields`/`farmEnergy`/`priority` — only `setNewPokemon()`/`evaluateMatchup()` do, and both only run for whichever Pokemon is currently ACTIVE. A bench member that stays benched all battle, or that was itself a LEAD in this same instance's immediately preceding `battleTeams()` call, carries those three fields over from that unrelated matchup. Direct proof: comparing the exact `4|4|2|2` flip built fresh vs. reached after 332 real prior battles found the bench members at `baitShields=0`/`priority=1` (leftover) vs pvpoke's own defaults `baitShields=1`/`farmEnergy=false`/`priority=0` (fresh), while the two ACTIVE leads' own pre-battle state was independently confirmed bit-identical in both — isolating the divergence to the bench fields. **Fix:** `battleTeams()` stamps pvpoke's own defaults on all six roster members right after `setRoster()`/`setTeam()`, before the lead-only `setNewPokemon` calls (so those still correctly override the two leads). Merged on top of the concurrent fire's `wrapRunScenario` (kept — real, harmless defense-in-depth, just not sufficient alone). Verified: new `test/teamBattle.test.js` "T20b, part 2" tests — (1) a mon warmed as team B's lead (deterministically `priority=1` via `Player`'s own player-index priority, not AI strategy) resets to pvpoke's exact defaults when reused as a bench member; (2) the literal `4|4|2|2` flip replayed exactly (332 prior battles then the flip battle) now matches a fresh-instance run bit-for-bit (winner, `survivorsHp`, turns) — both fail without the fix (confirmed by reverting locally), pass with it. `scripts/variance-study.mjs --candidates 5 --opponents 8 --shuffles 3 --seed variance-study`: **0/360 flips across all 4 orderings** (down from 1/360 in "reversed" only). A second, differently-scaled/seeded run (`--candidates 6 --opponents 10 --shuffles 3 --seed t20b-verify`, 540 battles/ordering): also **0 flips**. Combined: 0 flips across 3,600 battle-comparisons. `npm test` → **219/219 green** (217 prior, including the concurrent fire's 3 `wrapRunScenario` tests, + 2 new "part 2" tests); no existing test's expected values needed to change. **NOT fully closing the broader doctrine:** this is proven for single-threaded, sequential-list reordering. Whether it's also the full explanation for T15c's threaded-execution flip (worker-pool reordering, not list reordering) is unverified — left as an explicit follow-up (see `src/engine/README.md`'s Known limitation section for the precise boundary) rather than retiring `test/tournament.test.js`/`test/parallel.test.js`'s tolerance-based threaded-vs-serial doctrine on unverified strength.
  - **Follow-up closed, routine fire 2026-08-22 (see PROGRESS) — the box above already reads CHECKED; this closes the explicit "not fully closing" gap the same fire's own note left open.** Reviewing the just-landed fix before re-verifying it against threaded execution (as instructed) found a FOURTH field with the exact same leak shape as `baitShields`/`farmEnergy`/`priority`: `hasActed` is also constructor-defaulted (`false`, `Pokemon.js:109`) and also never reset for a bench member by `reset()`/`fullReset()` or by `Battle#step()`'s own per-turn `poke.hasActed = false` (`Battle.js:300`, only clears it for the two currently-ACTIVE `pokemon[]` slots). A stale `hasActed=true` can make `Battle#getTurnAction`'s `! poke.hasActed` gate (`Battle.js:749`) treat a just-switched-in mon as having already acted. Added to the SAME reset loop (`teamBattle.js` now also sets `mon.hasActed = false` alongside the 3 existing fields) — new `test/teamBattle.test.js` "T20b, part 3" test proves it directly (a `hasActed=true` sentinel does not survive being reused as a bench member; fails without the fix, confirmed by temporarily removing just that one line and reverting). With all 4 fields covered, ran the requested threaded re-verification: `evaluateTeams(ctx, {opts:{threads:2}})` vs serial on a real fixture collection now produce full-result-object-identical output (`avgHpMargin` included, via `assert.deepEqual`), and `test/tournament.test.js`'s 144-stage-3-battle threaded-vs-serial test (T15c's own original scale) now asserts exact `winRate` AND `avgHpMargin` equality instead of the previous `3/36` tolerance band — both pass. `scripts/variance-study.mjs` re-run across 4 different seeds/pool sizes with the complete 4-field fix: **0 flips out of 1341+ battles combined** (`--candidates 5 --opponents 8 --seed variance-study`: 0/360; `--candidates 6 --opponents 10 --shuffles 4 --seed mech3-stress2`: 0/540; two more sweeps: 0/216, 0/225). Per the ticket's own instruction, upgraded `test/teams.test.js`'s and `test/tournament.test.js`'s tolerance-based assertions to `assert.deepEqual`/exact equality and retired the "serial is the reference mode" doctrine across `src/engine/README.md`, `README.md`, and `PLAN.md` Rev 4. **No third, thread-specific mechanism found** — the same 4-field bench-state leak fully explains both single-threaded reordering and real threaded (worker-pool) execution divergence. Verified: `node --test test/teamBattle.test.js` → 14/14 (11 prior + 3 new: 2 from the concurrent fire's own "part 2," 1 new "part 3" for `hasActed`). `node --test test/teams.test.js test/tournament.test.js` → both green with the tightened assertions. `npm test` → 220/220 green (219 prior + 1 net new). No dependencies added; no vendor files touched.

## Battle-reality fitness — alignment, locked leads, closers (2026-08-22, Jaxon's directive after the 82-gen evolve run) — routine-eligible; design + rationale: **PLAN.md Rev 6, read it before any T27–T30 ticket**
Jaxon's observation: the sim favors consistent neutral-matchup mons (Stunfisk in 9/10 elites) because fitness averages lead pairings uniformly and ignores shield-advantage closing. The engine (vendor TrainingAI) already models counter-switching and shield decisions — the fix is measurement + SELECTION, never battle math (rule 4). Standing rules for this section: T27's findings gate T28–T30 (its fire must APPEND findings into T28/T29's ticket text — zero-memory fires can't read a session); `--fitness classic` (today's metric) must remain available in every ticket; defaults that change results get an A/B before flipping.

- [x] **T27 — (seq first) Alignment/shield ground-truth investigation + measurement harness.** Questions: (1) confirm what AI difficulty our headless path actually runs (T2 spec said highest — verify in `src/engine/teamBattle.js`/vendor `TrainingAI.js`, name the constant); (2) determine what per-battle data can establish the LEAD-EXCHANGE OUTCOME (which side's lead fainted first, or lead HP margin at first faint) from `battleTeams`' existing summary/return — extending OUR summary extraction by reading state off pvpoke's live objects is allowed, editing vendor is not; (3) new `scripts/alignment-study.mjs`: on a real seeded sample (suggest ≥5 candidates × 8 opponents × 9 pairings × ≥2 seeds ≈ 720+ battles), measure as-the-engine-plays-it: P(win | won lead exchange), P(win | lost lead exchange), overall lead→win conversion, plus shield-banking stats (how often a side ends battle having spent 0/1/2 shields, and win rate vs shields-remaining-at-end) — these numbers tell T29 how strong the snowball term should be; (4) write findings to PROGRESS **and append a condensed findings block directly into T28 and T29's ticket text below**. Verify: `node --test test/alignmentStudy.test.js` (tiny deterministic N) + `npm test` green.
  - **Done 2026-08-22 (routine fire, see PROGRESS).** (1) Confirmed: `DEFAULT_DIFFICULTY = 3` in `src/engine/teamBattle.js` is index 3 in vendor `aiArchetypes.json` = "Champion" (Novice=0, Rival=1, Elite=2, Champion=3) — the strongest archetype pvpoke ships, matching T2's "highest AI difficulty" spec, unchanged. (2) Extended `battleTeams()`'s returned `summary` (additive fields only, verified nothing does a snapshot/deepEqual on the whole `summary` object — only `survivorsHp` gets that treatment) with `leadFaintTurnA`/`leadFaintTurnB` (first turn each side's ORIGINAL lead's `.hp` reaches 0, polled once per turn-loop iteration off the live Pokemon instance — `null` if that lead never faints) and `shieldsRemainingA`/`shieldsRemainingB` (`Player#getShields()` at battle end, a real public pvpoke method). No vendor edits, no battle math added — pure read-off-live-state extraction. (3) `scripts/alignment-study.mjs` + `test/alignmentStudy.test.js` (5/5, tiny 2×2×9×1 scale) built on this, mirroring `scripts/variance-study.mjs`'s structure/CLI conventions. Two independent real runs on the curated meta pool at Champion difficulty (1,800 battles combined, `--seed variance-study` 5×8×9×2=720 and `--seed t27-verify` 6×10×9×2=1,080): see the condensed findings appended into T28/T29 below (also written to PROGRESS.md in full). Verified: `node --test test/alignmentStudy.test.js` 5/5, `npm test` 225/225 green (220 prior + 5 new), `node --test test/teamBattle.test.js` 14/14 unaffected by the additive summary fields. **Note for whoever picks up T28/T29 next:** this fire's findings were written against the ORIGINAL T28 design (home-grown asymmetric-shield s10/s21 1v1 sims); a concurrent Jaxon commit (`41f7215`) landed while this fire was in flight and revised T28 to use pvpoke's own vendored leads/closers/switches rankings instead (see T28's own text below) — the lead-exchange numbers below are unaffected (they're independent of how T28 computes closer scores), but the shield-banking numbers' framing has been adjusted accordingly rather than left referencing the abandoned `closerScore`/s10/s21 design.
- [x] **T28 — (seq after T27) Role scores from pvpoke's OWN leads/closers/switches rankings.** **REVISED 2026-08-22 by Jaxon: do NOT run our own asymmetric-shield 1v1s** — pvpoke already publishes role-specific rankings with its own scenario weightings, and they are ALREADY VENDORED: `vendor/pvpoke/src/data/rankings/all/{overall,leads,closers,switches,chargers,attackers,consistency}/rankings-<cp>.json` (orchestrator verified 2026-08-22: all seven categories present for cp 1500/2500/10000; 1,144 entries each at 1500; mimikyu is #1 in leads AND closers AND switches at 1500 — a good anchor). New `src/meta/roles.js`: `loadRoleScores(ctx, opts)` → per-speciesId normalized `{lead, closer, switch}` scores from the vendored files, cp-aware via `ctx.cp` (same path pattern usage.js follows post-T18b), plus the SAME optional live-refresh snapshot design as T9's usage loader (a cp-tagged committed snapshot preferred when present+valid; network failure NEVER breaks a run; NO network in tests). Document honestly in code + README: role scores are SPECIES-level priors under pvpoke's recommended movesets — instance-specific detail (user's IVs / current moves) still comes from our own matrix + real 3v3 results; the priors weight sampling/fitness, they don't replace battles. Surface: per-mon lead/closer/switch columns in the report appendix; loader is consumed by T29's fitness closer-term and lead-slot/lead-rotation weighting. Verify: `node --test test/roles.test.js` (normalization, determinism, cp-awareness at 1500 vs 2500, mimikyu-top-decile anchor in leads+closers at 1500, snapshot-preference + corrupt-snapshot fallback via temp file) + `npm test` green.
  - **T27 findings (condensed, 2026-08-22) — real ground truth from 1,800 real 3v3 battles on the curated meta pool at Champion difficulty (2 independent seeded runs, `scripts/alignment-study.mjs`, full numbers in PROGRESS.md).** Written against T28's ORIGINAL design (home-grown s10/s21 shield-advantage sims) before this fire discovered Jaxon's revision above; kept here because the underlying signal still validates that shields-remaining is worth surfacing as a role/closer factor, whichever way T28 sources it. The overwhelming majority of battles (≈85%+ of sides) end with 0 shields remaining on BOTH sides (shields mostly get spent in a typical game) — win rate there sits near the expected symmetric baseline (~0.49). Ending a battle with 1 shield still banked correlates with a meaningfully HIGHER win rate (~0.59–0.66 across the two runs) — real, consistent closer-value signal, a reasonable sanity check to run against pvpoke's own vendored `closers` ranking once T28's loader exists (do top-`closers`-ranked mons actually end more of OUR real 3v3 battles with a shield banked?). Ending with 2 shields remaining is rare at this sample size (only 7–10 sides per run) and noisy (0.43 in one run, 0.80 in the other) — plausible real signal, but this sample can't confirm its size.
  - **Done 2026-08-22 (routine fire, see PROGRESS).** `src/meta/roles.js`'s `loadRoleScores(ctx, opts)` reads the three vendored `rankings/all/{leads,closers,switches}/rankings-<cp>.json` files (confirmed identical ~1,144-species universe across all three at every supported cp before writing anything), returns `Map<speciesId, {lead, closer, switch}>` with each value = pvpoke's own 0-100 `score` field / 100 (NOT normalized to sum-to-1 like `usage.js`'s sampling weights — these are independent priors meant to be read directly, e.g. as a report column, not drawn from as a distribution). Same cp-aware path pattern as `usage.js` (post-T18b) and the same optional cp-tagged snapshot-preference design as T9 (`data/meta-roles.json`, corrupt/missing/wrong-cp snapshot falls back to vendored without throwing, no network in the loader itself) — **scope call: no `scripts/refresh-roles.mjs` fetch script was built**, since the ticket's own text only asked for "the same optional live-refresh snapshot design" (the loader-side preference logic), not a live-fetch companion; a snapshot file would need to be hand-authored or a future ticket could add the fetch script. Surfaced in the report appendix: `src/report/index.js`'s `ReportInput` gained an optional `roleScores` field (Map, absent → appendix renders byte-identical to before T28 — verified no existing report test needed updating) with new Lead/Closer/Switch columns in both `renderReport` (Markdown) and `renderReportHtml`; `src/cli.js` always computes `loadRoleScores(ctx)` (local file reads only, cheap) and passes it through. Manually verified end-to-end with a real CLI run (not just unit tests): appendix table showed real Lead/Closer/Switch percentages per mon. README gained a "Role priors: Lead / Closer / Switch" section. Verified: `node --test test/roles.test.js` → **10/10** (normalization/shape, missing-role-defaults-to-0, determinism, cp-1500-vs-2500 disagreement, mimikyu top-decile lead+closer anchor at 1500, snapshot preference, wrong-cp/corrupt/missing/badly-shaped snapshot fallback). `npm test` → **235/235 green** (225 prior + 10 new).
- [x] **T29 — (seq after T27, T28) Locked-lead teams + battle-reality fitness in the GA.** Per PLAN Rev 6: population individuals become (lead, back1, back2) — dedupe/identity by lead + back-set (same trio, different lead = different individual); NEW lead-rotation mutation (seeded chance to promote a back to lead; rate tunable + documented) alongside member-swap; evaluation battles each candidate with ITS OWN LEAD ONLY (≈3x fewer battles per matchup — document the reinvestment: more opponents or gens per wall-clock); opponent leads: curated/preset teams use their established lead where data provides one, else documented default; sampled opponents seeded (decide + document, per Rev 6). Fitness = documented tunable blend of locked-lead win rate + snowball term (using T27's extraction + measured coefficients) + closer term (T28), with `--fitness classic` preserving today's metric exactly. Checkpoint format: version it — refuse old-format checkpoints with a clear message (no silent corruption). Verify: `node --test test/evolve.test.js test/evolveScript.test.js` + `npm test` green.
  - **Part 2/N done by a CONCURRENT fire (2026-08-22T21:43Z — see PROGRESS): own-lead-only battle-driving + opponent-lead decision + checkpoint versioning landed in `scripts/evolve.mjs`, box left UNCHECKED.** This entry backfills GOALS.md on that fire's behalf — it updated PROGRESS.md but not this file. `pickLeadB`/`threeRandomPairings`/`ninePairings` replaced with `ownLeadPairing` (per-generation loop: candidate's `team[0]` vs. the opponent's resolved lead, one battle) and `ownLeadEliteSpread` (final elites pass: candidate's `team[0]` vs. all 3 of the opponent's possible leads, three battles — a deliberate departure from a same-fire alternative that would have kept elites at the old 9-pairing grid, since the elite's OWN lead no longer varies once locked). Opponent-lead resolution (`opponentLeadIndex`): checked pvpoke's vendored `gobattleleague/<cp>.json` presets and `data/meta-teams-community.json` directly — neither encodes a lead, so curated/preset teams default to member index 0, while sampled teams (composed by weighted random draw, no positional meaning to begin with) get a seeded-random lead keyed to the OPPONENT's own id (so the same sampled team has one stable lead across every candidate battling it within a generation) — an `opp.leadIndex` override hook is honored first for a future data source. `CHECKPOINT_FORMAT_VERSION = 2` + a `formatVersion` field on every checkpoint; the resume-scan throws a clear, actionable error on a config-matching-but-format-mismatched checkpoint instead of silently resuming. Real acceptance run confirmed the ~3x battle reduction directly (32 battles/generation at population=8×opponents=4, not 96). Verified: `node --test test/evolveScript.test.js` → 10/10 (8 prior + 2 new: a locked-lead bestLead-resolves-to-team[0] invariant check, a checkpoint-format-refusal test). `npm test` → 240/240 green.
  - **Part 3/N (fitness blend) done this fire (2026-08-22T21:56Z-ish, routine — see PROGRESS): box now CHECKED — this was the ticket's last remaining item.** New `--fitness classic|battle-reality` flag on `scripts/evolve.mjs` (default `classic` — deliberately NOT flipped, since T30's own text reserves the evolve-DEFAULT decision for itself, informed by its own A/B). `battle-reality` blends `winRate=0.6, snowball=0.3, closer=0.1` (documented judgment call, not directly derived from T27's ~0.70/0.30 conditional-probability numbers — those measure P(win|won exchange) vs P(win|lost), a different quantity than a fitness-component weight, but they're why snowball isn't a token term). **Snowball score**: per team, its own fraction of DECIDED lead exchanges (from `battleTeams`' summary `leadFaintTurnA`/`leadFaintTurnB`, T27's own extraction, free — no new battles) it won this generation, via a `leadExchangeLoser` helper duplicated verbatim from `scripts/alignment-study.mjs` per this file's own established small-helper-duplication convention; falls back to `winRate` when a team had zero decided exchanges (a tiny `--opponents-per-gen`, or an unusually stalemate-prone matchup) — a neutral choice that doesn't bias the blend either way when the sample can't measure it. **Closer score**: mean of T28's `loadRoleScores(ctx).get(speciesId).closer` prior across the team's two BACK members (`team[1]`/`team[2]`) — documented judgment call: closing out a game with a shield advantage is a back-line job under the locked-lead convention (T27's shield-banking findings), not the lead's. Both components are computed UNCONDITIONALLY (cheap) on every battle result regardless of `--fitness`, and surfaced in the report (new "Snowball score" / "Closer score" line in Elite team detail, both MD and HTML) even under `classic` fitness, so a report always shows how a team wins, not just whether. `fitness` added to `buildRunConfig`'s checkpoint fingerprint (a config-field change, correctly handled by the existing `configsMatch` resume gate — no `CHECKPOINT_FORMAT_VERSION` bump needed, that guard is for population-SHAPE changes specifically). **Incidental fixes, found while editing the exact same file/paragraphs** (not separate scope excursions): 3 leftover "full 9-lead-pairing" references in header/report-heading text that part 2/N's own fire missed updating when it changed the elites pass to `ownLeadEliteSpread` (the `--elites` flag doc comment, the Markdown report heading, the HTML report heading) — corrected to describe the actual "own lead vs. opponent's 3 leads" behavior; README.md's evolve section also still said "bottom 25%"/"×3 battles" (stale since Jaxon's `DEFAULT_DEATH_RATE=1/3` revision and part 2/N's own battle-count change) — corrected, plus new "Locked leads"/fitness/checkpoint-format sub-sections added there and to `PLAN.md` Rev 6 (a new dated amendment). Verified this fire: `node --test test/evolveScript.test.js` → **16/16** (13 prior + 3 new: battle-reality components always-computed + report-surfaced check, a `--fitness battle-reality` end-to-end run confirming the checkpoint fingerprint + a reconstructed-from-weights `blendFitness` check + report text, an unrecognized-`--fitness`-value rejection check). `node --test test/evolve.test.js test/evolveScript.test.js` → 26/26. `npm test` → **243/243 green** (240 prior + 3 net new). Two real acceptance runs (not just tests): `--fitness battle-reality` (population 8 × opponents 4 × 3 generations, seed t29blend-accept) — exit 0 in 16.2s, report's Settings line read `fitness: battle-reality (weights: winRate=0.6, snowball=0.3, closer=0.1)`, elite detail showed real Snowball/Closer percentages (e.g. "73% (lead-exchange win rate, 8W/3L decided)"), checkpoint's `config.fitness === 'battle-reality'` and `fitness[]` array held blended (non-winRate) values on disk; and a default `classic` run confirming byte-for-byte unchanged default behavior otherwise, Settings line reading `fitness: classic`. Both scratch output directories deleted afterward (`out/` gitignored). **Files touched:** `scripts/evolve.mjs` (`loadRoleScores` import, `DEFAULT_FITNESS_WEIGHTS`/`leadExchangeLoser`/`computeSnowballScore`/`computeCloserScore`/`computeBlendFitness`, `evaluateTeamsInOrder` gains `roleScores` param + unconditional exchange/closer/blend tracking on `entry`, `--fitness` CLI flag + `fitnessFlag` validator + `buildRunConfig` fingerprint field + `runEvolution`'s own opts.fitness guard, report Settings/elite-detail lines in both renderers, the 3 incidental "full 9-lead-pairing" fixes), `test/evolveScript.test.js` (+3 tests), `README.md` (evolve section rewrite), `PLAN.md` (Rev 6 amendment), `GOALS.md` (this backfilled part 2/N note + this part 3/N note, box checked), `PROGRESS.md` (this entry). **Dependencies:** none added. No vendor files touched.
  - **On the race, for the record:** this fire initially independently built its OWN version of part 2/N (own-lead-only battle-driving + opponent-lead decision + checkpoint versioning) before discovering, on `git fetch` immediately before committing, that a concurrent session had already landed an equivalent (and in two respects more polished — elites-pass consistency and a smarter per-sampled-team opponent-lead resolution vs. this fire's flatter always-index-0 default) version. Per this project's repeated standing precedent for exactly this situation (T20b/T23/T25/T26 races — compare before assuming "discard mine, done"), found no additive gap worth contributing back this time, discarded the local uncommitted work via `git checkout --`/`git reset --hard origin/main` rather than force-pushing a competing version, verified the landed commit's `npm test` clean on this machine, and moved on to the ticket's one remaining item (part 3, the fitness blend, above) instead.
  - **Part 1/N done this fire (2026-08-22T20:56Z, routine — see PROGRESS): the GA-core representation change, fully landed in `src/teams/evolve.js`, box stays UNCHECKED — the battle-driving half (own-lead-only evaluation, fitness blend, checkpoint versioning) in `scripts/evolve.mjs` is NOT started.** `team[0]` is now the locked-lead convention (`team[1]`/`team[2]` are the unordered backs); `teamSignature` (the one function every dedup/uniqueness check in the module routes through — population fill, mutant/immigrant collision checks, convergence's top-N set) is now `(lead, sorted(backs))` instead of a fully-sorted species-set, so the SAME 3 species with a DIFFERENT lead is correctly treated as a different individual everywhere, for free. `initPopulation` assigns each freshly-sampled (still-unordered) `sampleCandidateTeams` team a seeded-random lead via a new internal `assignLead` helper; `nextGeneration`'s immigrant draw does the same before its signature check (immigrants are freshly unordered too). NEW mutation type `leadRotation` (promote a uniform-random back to lead, demoting the old lead into that back slot — same species-set, different individual) alongside the existing `memberSwap`, split by a new tunable `DEFAULT_LEAD_ROTATION_RATE = 0.3` (opts.leadRotationRate) — documented judgment call, no value was specified upstream; picked so lead-assignment is genuinely explored without displacing member-swap's pre-existing primacy. `rollMutations` now rolls TYPE via a second, immediately-following rng() draw on a successful mutation roll only (deterministic, same fixed survivor order as before). Lineage entries for mutants now carry `mutationType: 'memberSwap'|'leadRotation'` plus the type-appropriate `swappedSlot`/`promotedSlot` field (origin values themselves — `'survived'|'mutant'|'immigrant'` — are UNCHANGED, so `scripts/evolve.mjs`'s `computeGenerationAnalytics`'s `originCounts` tally needed zero changes). **Deliberately backward-compatible with the existing driver:** `scripts/evolve.mjs` still treats population entries as plain `string[3]` arrays and evaluates/reports across all 3 own-leads (unaware `team[0]` is now special) — this is NOT yet the ~3x battle-count saving the ticket wants, that requires actually changing the battle-driving loop, which is explicitly deferred to the next fire on this ticket. Verified this fire: `node --test test/evolve.test.js` → 15/15 (13 prior, restructured where the mutant-shape/uniqueness assertions needed lead-aware updates, + 2 new: lead-rotation-rate statistical mix check, initPopulation lead-randomization-over-60-seeds check, plus a same-species-set-two-leads-coexist check folded into the same test group). `node --test test/evolveScript.test.js` → 8/8 unaffected (proves the representation change is genuinely backward-compatible, not just "tests still pass because nothing exercises it"). `npm test` → **238/238 green** (236 prior + 2 net new). **Next fire should NOT re-derive this — pick up from:** (1) wire `scripts/evolve.mjs`'s per-generation battle loop (`evaluateTeamsInOrder`) to battle each population member ONLY at `team[0]` (drop the `LEADS.map` own-lead loop, keep only `pairingsFor`'s opponent-lead variation) — the ~3x saving; (2) decide + document opponent leads (curated/preset teams' "established lead where data provides one" — check what `data/meta-teams-community.json`/`vendor/pvpoke` training presets actually carry per-team lead info, if any; else a documented default, e.g. always opponent's `team[0]`/index 0); (3) fitness blend: T27's snowball coefficients (~0.70/~0.30, see T29's own condensed findings block below) + T28's `loadRoleScores(ctx).get(speciesId).closer`, with `--fitness classic` as an escape hatch to today's plain win-rate metric; (4) checkpoint format versioning (a `formatVersion` field in `evolve-gen<N>.json`, refuse-with-clear-message on mismatch) since this fire's representation change already makes OLD checkpoints (population entries with no defined lead-slot convention) semantically stale — no checkpoint compat code was written this fire, so an old checkpoint would currently be silently misread as if `team[0]` were already a valid lead, which is exactly the silent-corruption risk T29 asks to guard against; flag this prominently, don't let a stale checkpoint slip through un-versioned.
  - **T28 API note for whoever picks this up (condensed, 2026-08-22):** the closer-term data source is shipped and ready to consume — `import { loadRoleScores } from '../src/meta/roles.js'`; `loadRoleScores(ctx, opts)` → `Map<speciesId, {lead, closer, switch}>`, each in `[0,1]`, cp-aware via `ctx.cp` (already matches whatever `--cp` this evolve run is using). No battles involved, cheap to call once per run. `entry.closer` is the direct input for the closer term this ticket's own text asks for; `entry.lead` may be useful input for the lead-rotation mutation's proposal distribution (not mandated — this ticket's own design call). A species not covered by the loader returns `undefined` from `.get()` (never happens for a real gamemaster speciesId under the pinned vendor commit, but guard for it rather than assuming presence).
  - **T27 findings (condensed, 2026-08-22) — real ground truth, same 1,800-battle sample as above:** the lead exchange (one side's original lead fainting strictly before the other's) is DECIDED in ~78–83% of battles; ~2% end in a simultaneous double-faint (no clear winner); ~15–21% never see either lead faint at all (game decided by bench mons, or timeout with both leads alive) — plan for a real "no exchange" / "simultaneous" case in the fitness formula, not just a binary won/lost. Conditional on a decided exchange: **P(win | won lead exchange) ≈ 0.69–0.73; P(win | lost lead exchange) ≈ 0.27–0.31** — consistent across both independent runs. This is a STRONG, large snowball signal (winning the lead exchange is roughly a 2.3–2.7x multiplier on win probability vs. losing it) — the snowball term should carry substantial weight in the fitness blend, not a token one, and measured coefficients (~0.70 / ~0.30) are a reasonable starting anchor for calibrating it rather than an arbitrary guess.
- [x] **T31 — (seq BEFORE T30, deliberately out of numeric order: these are real opponents and must be in the curated pool before T30's A/B runs so the A/B field includes them) "Real teams fought" — 7 ladder teams from Jaxon (2026-08-23), first-listed Pokemon IS the lead.** Add to `data/meta-teams-community.json` as a new provenance group: ids `jaxon-ladder-1`…`jaxon-ladder-7`, names like "Real team fought N (Jaxon's GBL ladder, Aug 2026)", members lead-first. **PLUS a second directive from Jaxon (2026-08-23), which makes this file-wide doctrine: ALL curated teams — every entry in this file AND the vendored pvpoke gobattleleague presets — treat `members[0]` as the established lead.** T29 part 2/N's driver already lands on member index 0 for curated/preset teams, but only as a silent fallback default; make it explicit: the community-teams loader (follow where `data/meta-teams-community.json` is read, likely `src/meta/teams.js`) stamps `leadIndex: 0` on every loaded team so `scripts/evolve.mjs`'s existing `opp.leadIndex` override hook fires as declared data rather than falling through to the default, and the same doctrine is documented for the vendored presets path (confirm the driver battles presets at member index 0 too — it does per T29's notes; just make the doc comment say "first member IS the lead, per Jaxon 2026-08-23" instead of "no source encodes a lead"). Add a loader test proving a `jaxon-ladder-*` team AND one legacy community team battle with `members[0]` as lead. Teams as Jaxon gave them (lead / back / back), with best-guess speciesIds — **verify every id against the vendored gamemaster before committing** (file convention: skip-with-warning + drop the whole team if any member fails to resolve; that convention is for load-time — at authoring time, resolve correct ids so nothing drops):
  1. Talonflame / Greninja / Empoleon → `talonflame`, `greninja`, `empoleon`
  2. Morpeko / Corviknight / Quagsire → Morpeko is likely `morpeko_full_belly` (verify), `corviknight`, `quagsire`
  3. Clefable / Kingdra / shadow KANTO Ninetales → `clefable`, `kingdra`, `ninetales_shadow` (NOT `ninetales_alolan_shadow` — Jaxon explicitly said Kanto; note this in `notes` like the existing Stunfisk entry does)
  4. Forretress / Thievul / Azumarill → `forretress`, `thievul`, `azumarill`
  5. Greninja / Gourgeist (Super size) / Kommo-o → `greninja`, `gourgeist_super` (verify size-form id), `kommo_o` (verify punctuation-mangled id)
  6. Venusaur / SHADOW Drifblim / Gardevoir [Jaxon observed fast move CONFUSION, not the usual Charm] → `venusaur`, `drifblim_shadow`, `gardevoir` + `notes` recording the Confusion observation; if curated opponents support per-member move overrides, apply it — if they don't (likely: opponents use pvpoke recommended movesets), record honestly in notes + PROGRESS that the sim will play Gardevoir with its default moveset, do NOT build a move-override system for this ticket
  7. Hisuian Electrode / Jellicent / SHADOW Feraligatr → `electrode_hisuian` (verify), `jellicent`, `feraligatr_shadow`
  Weighting: leave untagged (full meta weight, NOT `tier:'off-meta'`) — documented judgment call: these are opponents Jaxon actually faces on ladder, exactly the field to train against. Update the file's `extraction` header blurb: new provenance group + the file-wide lead doctrine (member order IS lead-bearing for ALL entries per Jaxon 2026-08-23 — first member = lead; for the 46 legacy entries that order came from the source images, best available; for `jaxon-ladder-*` it is his directly-observed opponent lead). Also update the `opponentLeadIndex` doc comment in `scripts/evolve.mjs` — its "no opponent-team source in this codebase encodes an explicit lead" claim becomes false the moment this lands. Verify: the loader test above + `npm test` green.
  - **Done 2026-08-24T02:31Z (routine fire, see PROGRESS).** All 7 teams added to `data/meta-teams-community.json` under ids `jaxon-ladder-1`..`jaxon-ladder-7`, untagged (meta, full weight), members lead-first exactly as Jaxon gave them. Every speciesId verified against the pinned gamemaster BEFORE writing (`morpeko_full_belly` not bare `morpeko`; `ninetales_shadow` not `ninetales_alolan_shadow` per Jaxon's explicit Kanto call-out; `gourgeist_super` and `kommo_o`, both confirmed present) — all 7/7 resolved fully battle-ready, none dropped. Team 6's Gardevoir-Confusion observation recorded in `notes` only, exactly as the ticket instructed (no move-override system built; the sim plays Gardevoir's pvpoke-recommended moveset). File-wide lead doctrine landed as CODE, not just documentation: `src/meta/teams.js`'s `loadCommunityTeams` now stamps `leadIndex: 0` on every returned team (all 53 entries, not just the 7 new ones) so `scripts/evolve.mjs`'s pre-existing `opp.leadIndex` override hook (added by T29 part 2/N as a "forward-compatible hook... honored if a future opponent-data source ever adds an explicit lead") now actually fires as declared data instead of silently falling through to its own index-0 default — `opponentLeadIndex()`'s doc comment and the header's LOCKED LEADS note updated accordingly (no behavior CHANGE for existing curated teams, since the fallback default was already 0; this makes it explicit/declared rather than implicit). `buildVendorTeams`' own doc comment updated to state pvpoke's gobattleleague presets share the same doctrine (no `leadIndex` field needed there — nothing consumes it yet besides evolve.mjs's own already-0 default). Extraction header blurb rewritten with the new provenance group + the full file-wide doctrine statement. New loader tests in `test/metaTeams.test.js`: every community team carries `leadIndex: 0`; all 7 jaxon-ladder teams resolve + are untagged/full-weight; a jaxon-ladder team battles a legacy community team with both sides led by `members[0]` via a real `battleTeams` call (not just an assertion on the data shape). Pre-existing test's hardcoded counts updated (46→53 total entries, floor 39→46 resolved, both re-derived from the actual loader output before writing, not guessed). Verified: `node --test test/metaTeams.test.js` → **15/15** (12 prior + 3 new). `npm test` → **246/246 green** (243 prior + 3 net new), ~200s full suite wall time. No engine/scoring/battle-driving files touched; no dependencies added.
- [ ] **T30 — (seq after T29 and T31) Reports + A/B validation.** Reports render teams as "Lead / Back / Back", add per-team snowballIndex/comebackIndex + designated closer (from T28 data); analytics JSON carries the new fields. ACCEPTANCE A/B (the deliverable Jaxon asked for): two small-but-real evolve runs, same seed/collection/opponents, `--fitness classic` vs battle-reality — side-by-side top-10 tables + which species rose/fell recorded in PROGRESS. Also decide + document (with the A/B as evidence) whether battle-reality becomes the evolve DEFAULT. Verify: `npm test` green + both A/B run artifacts in out/ + the comparison in PROGRESS.
