# ROADMAP — the why

**North star:** Jaxon feeds in a CSV of their actual Pokemon; the tool tells them the best Great League (CP ≤ 1500) teams of 3 they can field, validated by pvpoke's real battle engine running full **3v3 team battles** against the current meta — not eyeballed rankings, not hand-waved coverage scores.

**Definition of done (MVP):** `node src/cli.js their-export.csv` produces a ranked report of teams with team-battle win rates vs meta teams, best leads, and hardest matchups — end-to-end green tests, README a stranger could follow.

## Known gaps / backlog (pull from here when GOALS.md empties) — dated 2026-08-20
- **Best Buddy detection**: Poke Genie exports carry no Best Buddy column, so level-51 eligibility is invisible; importer recognizes a `Best Buddy` column opportunistically. Gap: document a manual override (e.g. generic-CSV bestbuddy column or a flags file).
- **Current-moves mode**: rank using each mon's actual TM state instead of recommended movesets (Poke Genie has move columns; reliability was the reason this was deferred).
- **Shield-scenario weighting review**: 0.25/0.50/0.25 for the 1v1 matrix was a judgment call; revisit once 3v3 results exist to compare against.
- ~~**HTML report** (nicer than out/report.md)~~ — done (GOALS T16, 2026-08-21): `out/report.html` written by default alongside `out/report.md`. A published status/report page (hosting it somewhere, auto-refresh, etc.) is still open if wanted later.
- **--cp 2500 / Ultra League flag**. **Correction (2026-08-21):** this gap's original note ("engine already parameterizes the cap internally; expose it end-to-end") does not hold up — investigated ahead of picking this up and found CP 1500 hardcoded in several places, not one: `src/engine/harness.js`'s `GREAT_LEAGUE_CP` constant and its hardcoded `rankings-1500.json` path (cup is never set explicitly either, relying on `Battle()`'s own CP-1500 default), `src/scoring/index.js`'s `defaultCp1500Ivs` (reads gamemaster's `defaultIVs.cp1500` specifically), and `src/meta/teams.js`/`src/meta/usage.js`'s hardcoded `.../1500.json` data file paths. Vendor DOES have the CP-2500 (Ultra League) equivalents of all of these already in the sparse-checked-out data (`rankings/all/overall/rankings-2500.json`, `groups/ultra.json`, `training/teams/gobattleleague/2500.json` — confirmed present in `vendor/pvpoke/src/data`), so the data isn't the blocker — but wiring a `--cp` flag through cleanly touches engine/scoring/meta/CLI/report, more than a one-fire chunk. Left for a future fire to scope properly rather than started and left half-done this fire.
- **Usage-weighted meta**: absorbed into the sampling initiative (GOALS T9–T12, 2026-08-21) — usage weights now power both opponent- and candidate-team sampling.
- **Safe-swap analysis**: per recommended team, which member is the safest first switch.
- ~~**TrainingAI variance study**~~ — quantified (2026-08-21): `scripts/variance-study.mjs` + `test/varianceStudy.test.js`. First established that AI randomness itself is NOT the variance source: `src/engine/teamBattle.js` already patches the vm's `Math.random` with a seeded mulberry32 PRNG derived per battle from (teams, leads, seed), so vendor/pvpoke's own `TrainingAI.js` `Math.random()` calls (energy estimates, move-option picks, IV-combo choices, buff rolls) are fully pinned — `test/bench.test.js` already verified bit-identical results for a FIXED battle order under a fixed seed. The real, order-dependent variance source is the `resetMoves()`/`bestChargedMove` stale-`self.index` mechanism GOALS T15b/T15c found (see `src/engine/README.md`'s "Known limitation" section). This script quantifies it directly: runs the SAME real battle set (5 candidates × 8 opponents × all 9 lead pairings = 360 battles) through 4 different seeded-deterministic orderings (a full reversal + 3 shuffles) vs a canonical baseline, on real curated meta teams. **Measured result:** each alternate ordering flipped exactly 1/360 battles (0.28% flip rate, consistent across all 4 orderings — suggests one specific matchup sits right at a moveset tie-break knife-edge), max win-rate delta 1.4 percentage points, and **team RANKING never changed** across any ordering. Consistent with T15c's own by-hand finding (1 flip in a 90-battle sample) at a larger, more systematic scale. Practical takeaway: order-sensitivity is real but small and did not move which team a funnel/report would recommend in this sample — worth re-running at larger scale (more candidates/opponents, more shuffle seeds) if more confidence is wanted, but the qualitative finding (rare, bounded, rank-preserving in practice) is now backed by a repeatable, checked-in tool instead of an anecdote.
- **Vendor refresh discipline**: bump the pvpoke pin on a cadence (meta shifts each season); re-run T0 validation after any bump.
